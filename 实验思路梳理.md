# Exp1：SAM3 在 Potsdam 上的零样本基线实验

Exp1 的核心思路是：不对 SAM3 做任何遥感数据微调，直接使用 Potsdam 官方六类语义名称作为文本 prompt，让 SAM3 在高分辨率遥感影像上进行零样本语义分割。它回答的是一个最基础的问题：通用开放词汇分割模型能否直接迁移到遥感密集语义分割任务中。

在整个论文实验体系中，Exp1 的作用是建立起点和问题动机。它不是为了追求高精度，而是用来证明“单纯依赖 SAM3 zero-shot 不能稳定完成 Potsdam 这类遥感语义分割任务”。因此，Exp1 为后续 Exp2 的 prompt 设计、Exp3 的多模态输入、Exp4 的监督语义模型，以及 Exp5 的 SegFormer + SAM3 推理分割体系提供了必要性依据。

一句话概括：Exp1 证明了直接 zero-shot 的局限，从而引出“需要构建面向遥感任务的推理分割体系”这一论文主线。

# Exp2：Prompt 构造与 mask 过滤机制实验

Exp2 系列实验是在 Exp1 零样本基线基础上的进一步拆解。它不再只问“SAM3 直接能不能分割 Potsdam”，而是开始分析开放词汇分割模型在遥感任务中失败的具体原因：类别 prompt 是否合适、背景类是否应该主动提示、多个视觉化 prompt 是否能提升候选 mask 质量，以及 mask 过滤是否能降低噪声。

Exp2-A 的思路是去掉主动的 `clutter/background` prompt，只对前景类别进行提示，把背景视为 residual 类别。实验结果并没有带来精度提升，而是与 Exp1 基本一致；它的价值在于说明背景过预测并不是由主动输入 `clutter/background` prompt 直接造成的，而主要来自前景 mask 覆盖不足以及未覆盖像素回退到背景类。

Exp2-B 的思路是为每个前景类别设计多个更视觉化、更接近日常视觉概念的 prompt，例如 building / rooftop、road / pavement、car / vehicle 等，并将多个 prompt 的候选 mask 集成。它用于验证：开放词汇模型对 prompt 表达非常敏感，官方类别名不一定是最适合模型理解的视觉概念。

Exp2-B 的结果说明，视觉化 prompt ensemble 是有效的。相比 Exp1 / Exp2-A，mIoU 从 0.0578 提升到 0.1052，Mean F1 从 0.1020 提升到 0.1806，valid masks 从 6823 增加到 23066。这表明，将遥感类别转换为更具体、更常见的视觉概念，确实可以让 SAM3 生成更多前景候选 mask，改善前景覆盖。

但 Exp2-B 也说明，仅靠 prompt 工程仍然不够。预测为 `clutter/background` 的像素比例虽然从 78.89% 降到 69.38%，但仍远高于真实标签中的 4.65%。这说明大量道路、建筑、植被等真实前景区域仍然没有被 SAM3 有效覆盖，最终继续 fallback 到背景类。因此，Exp2-B 的结论应理解为：prompt ensemble 有效，但不足以让 SAM3 独立完成高质量遥感密集语义分割。

Exp2-C 的思路是在 Exp2-B 的 prompt ensemble 基础上，加入固定的、类别相关的 mask 面积比例过滤。也就是说，在按 score 融合之前，先根据每个类别预设的最小/最大面积比例，剔除过小或过大的候选 mask。它并没有加入边界质量、mask 稳定性、类别竞争、重叠去重或遥感先验等更复杂的质量判断。

Exp2-C 的结果说明，朴素的固定面积过滤没有带来进一步收益，反而相对 Exp2-B 略微降低了 OA、mIoU、Mean F1 和 FWIoU。主要原因是过滤集中作用于 `impervious surface` 的大面积 mask，而在城市遥感图像中，不透水表面本身可能形成道路、广场、停车场等大范围连续区域；简单面积上限会误删有用前景候选，使更多像素回退到 `clutter/background`。

因此，Exp2-C 不能被概括为“mask 过滤无效”，而应理解为：仅靠固定面积阈值不足以判断 SAM3 mask 质量。后续如果要做 mask 质量控制，需要更稳健的信号，例如边界一致性、mask 稳定性、类别竞争关系、图像纹理、多源遥感先验或监督语义模型先验。

Exp2-C 之所以没有直接加入完整的 mask 质量过滤，是因为它仍处在 SAM3 zero-shot / prompt 工程阶段，实验目标是单独验证“固定面积先验”这个简单因素是否有帮助。如果此时同时加入边界、形状、类别冲突、SegFormer 一致性、NDVI/DSM 等多个条件，实验变量就会混在一起，无法判断到底是哪一类质量信号起作用。

更完整的 mask 质量判断应放到后续推理分割体系中逐步验证。事实上，后面的 Exp5-A 到 Exp5-F 就是在更系统地展开这一问题：用 SegFormer 不确定性定位风险区域，用 candidate region 限定搜索空间，用 SAM3 生成局部 proposal，再通过保守 refinement 和 proposal audit 判断哪些 mask 适合作为对象候选、哪些只能作为错误解释，哪些不应自动写入最终语义图。因此，Exp2-C 的价值不是完成 mask 质量过滤，而是证明朴素面积过滤不够，从而引出后续更完整的 proposal quality audit 与 reasoning package。

在整个论文实验体系中，Exp2 的作用是把 Exp1 中“zero-shot 效果不好”这个笼统结论拆解成可解释的推理分割因素。它说明，遥感任务中的开放词汇分割不能只依赖模型本身，还需要概念提示设计、背景处理策略和候选 mask 质量控制。

一句话概括：Exp2 证明 prompt、背景处理和 mask 筛选是遥感推理分割体系中的关键组成，而不是简单的参数调优。

# Exp3：多源遥感输入模态实验

Exp3 系列实验的核心问题是：在固定当前最有效的 Exp2-B 视觉化 prompt ensemble 策略后，仅改变输入模态，是否能改善 SAM3 在 Potsdam 上的候选 mask 生成和语义分割结果。也就是说，Exp3 不再继续改变 prompt 或面积过滤，而是专门检验 RGB、IRRG、RGBIR 派生三通道输入等多源遥感模态对 SAM3 的影响。

Exp3-A 是 RGB 输入参照组。它沿用 Exp2-B 的五个前景类别 prompt ensemble，不主动 prompt `clutter/background`，也不启用 Exp2-C 的面积过滤。它的作用是为后续 IRRG 和 RGBIR 输入提供同一 dev split 下的干净对照。结果显示，RGB 仍然延续了 Exp2-B 的基本行为：能够生成一定前景候选，但整体性能仍低，大量真实前景仍会 fallback 到背景类。

Exp3-B 将输入从 RGB 改为 IRRG 假彩色影像。相对于 Exp3-A，它的核心实验变量就是输入源从 RGB 变为 IRRG，其他设置保持一致：同一 dev split、同一 prompt ensemble、不主动 prompt `clutter/background`、不启用面积过滤、同样的 patch / stride / score 融合与 fallback 逻辑。实验假设是，近红外信息可能有助于植被、树冠等遥感类别。但结果表明，IRRG 在当前 SAM3 + prompt ensemble 设置下明显低于 RGB：有效 mask 数从 12402 降至 8171，mIoU 从 0.1032 降至 0.0629，Mean F1 从 0.1781 降至 0.1098。尤其 `low vegetation` 没有从近红外输入中受益，反而几乎失去有效召回。这说明问题不在于数据中是否含有 NIR 信息，而在于 SAM3 的视觉-文本表征是否能理解这种假彩色遥感外观。

Exp3-C 使用 RGBIR 四波段影像派生出的三通道 `nir-r-g` composite。这里并不是让 SAM3 接收真正的四通道 RGBIR 输入，因为 SAM3 仍然只能接收三通道图像；实际做法是从 RGBIR 的 R、G、B、NIR 四个波段中选取并重排 NIR、R、G，构造为 `[NIR, R, G]` 三通道假彩色输入。结果与 Exp3-B 非常接近，mIoU 为 0.0632，Mean F1 为 0.1104，略高于 IRRG 但显著低于 RGB。它说明，简单将 RGBIR 重排或派生成三通道假彩色输入，并不能恢复 SAM3 对自然 RGB 图像的表征优势，也不能有效提升植被类分割。

因此，Exp3-B 和 Exp3-C 在模型实际看到的通道语义上几乎没有本质区别：二者都是把近红外、红光、绿光编码成三通道假彩色图像。它们的主要差别在于数据来源和生成路径：Exp3-B 使用 Potsdam 官方提供的 IRRG 三通道影像，Exp3-C 则从 RGBIR 四波段影像中手动派生 NIR-R-G composite。Exp3-C 的价值不是提出一种全新的输入模态，而是作为确认性对照，排除“官方 IRRG 文件或预处理路径导致效果差”的可能性。两个实验结果高度接近，进一步支持一个结论：问题来自 NIR 假彩色输入与 SAM3 原有 RGB 视觉表征之间的不匹配，而不是某一个具体 IRRG 文件源的问题。

Exp3 的总体结论是：对 SAM3 这类主要基于自然图像视觉-文本对齐的开放词汇模型，RGB 仍然是最稳定的输入。IRRG 或 RGBIR 派生通道虽然具有遥感物理意义，但直接替代 RGB 输入 SAM3 并不会自动带来增益，甚至会破坏模型原有的视觉概念对齐。近红外信息不应简单作为 SAM3 的图像输入替代项，而更适合作为后续遥感先验、监督模型输入或质量审计信号。

在整个论文实验体系中，Exp3 的作用是排除“只要换成多源遥感输入，SAM3 就能显著变好”这一假设。它进一步说明，SAM3 的瓶颈不是单纯缺少 NIR 通道，而是开放词汇模型与遥感密集语义分割任务之间存在结构性差距。因此，后续更合理的方向不是继续在 SAM3 输入模态上硬调，而是引入监督语义模型、候选区域定位、多源遥感先验和解释性输出体系。

一句话概括：Exp3 证明 RGB 是 SAM3 在 Potsdam 上最稳定的输入，IRRG / RGBIR 直接替代 RGB 没有收益；多源遥感信息应作为先验或监督模型输入，而不是简单塞给 SAM3。

# Exp4：SegFormer RGB 监督语义基线实验

Exp4 的核心思路是：在 Exp1-Exp3 已经证明 SAM3 zero-shot / prompt-driven 方案难以独立完成 Potsdam 密集语义分割后，引入一个标准监督语义分割模型 SegFormer，训练一个稳定的 RGB dense semantic baseline。它回答的问题不再是“SAM3 通过 prompt 能不能直接分好”，而是“如果有监督语义模型提供完整像素级预测，它能否成为后续 SAM3 融合体系中的 coarse semantic prior”。

这个实验使用 Potsdam RGB 图像和 6 类标注训练 SegFormer-B0。与 SAM3 系列实验不同，Exp4 使用了训练标签，因此它不是 zero-shot 实验，也不应与 Exp1-Exp3 直接当作同一条件下的排行榜比较。它的作用是建立一个可靠的监督上限参照和语义底座，用来判断：SAM3 的合理位置到底是独立分割器，还是候选 mask / 边界 proposal / 错误解释模块。

Exp4 的结果显示，SegFormer RGB baseline 已经能够稳定完成 Potsdam 的主要地物语义分割。heldout full-tile evaluation 上，模型取得 OA 0.9021、mIoU 0.7687、Mean F1 0.8535，远高于前面所有 SAM3 zero-shot / prompt 输入实验。这说明对于 Potsdam 这类高分辨率遥感密集语义分割任务，监督语义模型能够提供 SAM3 单独无法提供的完整、连续、类别稳定的像素级预测。

但 Exp4 也暴露了监督模型自己的短板：`clutter/background` 是 heldout 上明显较弱的类别，IoU 只有 0.3635，主要问题是欠召回。也就是说，SegFormer 对 building、impervious surface、tree、low vegetation、car 等主要前景类已经比较稳定，但对语义杂乱、边界不清、样本占比低的背景/杂类区域仍容易吸收到前景类别中。

因此，Exp4 对论文主线的意义非常关键：它把研究方向从“让 SAM3 单独完成六类密集语义分割”转向“以监督语义模型给出稳定 coarse prediction，再用 SAM3 和多源先验处理不确定区域、边界区域、候选对象和错误解释”。这也避免了论文路线变成单纯调 prompt 或单纯替换输入模态，而是形成更合理的 coarse-to-fine 推理分割体系。

在整个论文实验体系中，Exp4 的作用是提供 dense semantic prior。SegFormer 负责输出完整、稳定的语义图；SAM3 不再被期待独立覆盖整图六类，而是更适合作为局部 proposal、候选 mask、边界细化或解释性审计工具。后续 Exp5 正是在这个基础上继续验证：哪些 SegFormer 错误区域值得交给 SAM3，SAM3 生成的 mask 是否能安全改写最终预测，以及它更适合作为自动 refinement 还是 reasoning package 的一部分。

一句话概括：Exp4 证明监督 SegFormer 可以提供稳定的遥感 dense semantic prior，也明确了 SAM3 在后续体系中的合理角色不是替代 SegFormer，而是围绕不确定区域和候选对象进行辅助推理。
