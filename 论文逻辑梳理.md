# 论文逻辑梳理：面向水利智能感知的 SAM3 开放词汇推理分割方法

## 1. 当前项目定位

本项目当前可以理解为一篇论文的技术准备工程，核心目标不是单纯实现一个 Potsdam 数据集语义分割脚本，而是围绕 **SAM3 开放词汇分割能力**，探索其在遥感图像理解和水利智能感知任务中的应用逻辑。

从已有文档和代码看，项目经历了两个阶段：

```text
第一阶段：SegFormer + SAM2-LoRA + 多类别融合
第二阶段：SAM3 开放词汇概念分割 + 水利知识约束 + 推理分割框架
```

其中，第一阶段更像传统语义分割增强方案：先用语义分割模型获得粗分割，再用 SAM2-LoRA 进行边界细化。第二阶段则将研究重心转向 SAM3：利用其文本概念驱动的开放词汇检测与分割能力，让模型根据自然语言概念直接发现目标，再结合水利专业知识进行约束、修正和解释。

因此，本文更适合定位为：

```text
面向水利智能感知的 SAM3 开放词汇推理分割方法研究
```

或：

```text
基于 SAM3 与水利知识约束的遥感图像推理分割方法研究
```

本文的核心不应只写成“将 SAM3 用于 Potsdam 数据集分割”，而应写成：

```text
以 SAM3 开放词汇分割能力为基础，
面向水利工程遥感监测和灾害识别任务，
构建一种融合自然语言概念、视觉分割模型与水利专业约束的推理分割框架。
```

## 2. 论文核心问题

### 2.1 传统语义分割的局限

传统遥感语义分割模型通常解决的是固定类别像素分类问题，例如：

```text
这个像素属于建筑物、道路、树木、水体，还是背景？
```

这类方法一般依赖大量标注数据，并且类别集合在训练前已经固定。对于水利场景而言，这种范式存在明显不足：

1. 水利目标类别复杂，例如洪水、漫溢水体、河道、库区、消落带、堤坝裂缝、渗漏区、滑坡堰塞体等。
2. 很多水利目标不是单纯视觉概念，而是由任务语义和专业知识共同定义。
3. 同一视觉外观可能对应不同水利含义，例如暗色区域可能是水体、阴影、湿地、屋顶或沥青路面。
4. 传统模型难以理解用户的自然语言任务需求，例如“只提取新增淹没区，不包括历史常水体”。
5. 固定类别模型无法灵活适应新任务、新概念和突发灾害场景。

### 2.2 SAM / SAM2 类方法的不足

SAM 和 SAM2 具备较强的通用分割能力，但它们通常需要点、框或 mask prompt。对于大规模遥感图像或水利灾害监测任务，人工 prompt 成本高，自动 prompt 生成又依赖额外模型。

SAM2-LoRA 方案可以提升遥感域适应能力，但其基本范式仍然偏向：

```text
粗分割或人工标注生成 prompt
→ SAM2 输出局部 mask
→ LoRA 适配领域边界特征
```

这种方案适合做边界细化，但不擅长直接回答开放语义问题，例如：

```text
找出可能由河道漫溢造成的新增积水区域。
```

### 2.3 SAM3 带来的机会

SAM3 的关键价值在于：它不只是交互式分割模型，而是具备文本概念驱动的开放词汇检测与分割能力。

它更适合回答：

```text
在当前文本概念或视觉提示下，图像中哪些区域符合该概念？
```

这使得水利任务可以被转化为概念分割问题。例如：

```text
flood water
river water
reservoir water surface
flooded road
flooded farmland
dam crack
seepage area
landslide scar
blocked river channel
```

因此，本文的核心问题可以概括为：

```text
如何利用 SAM3 的开放词汇分割能力，
并结合水利专业知识约束，
实现面向水利遥感监测和灾害识别的推理分割？
```

## 3. 论文核心观点

本文应围绕一个核心判断展开：

```text
水利智能感知中的分割任务，不应被视为单纯的像素分类问题，
而应被视为由任务语义、视觉证据和水利知识共同驱动的推理分割问题。
```

换言之，传统语义分割关注：

```text
像素属于哪一类？
```

而本文关注：

```text
在当前水利任务语境下，哪些区域符合目标概念，为什么符合，如何校正，如何解释？
```

这一转变是论文的主要逻辑基础。

## 4. 论文拟解决的研究问题

论文可以明确提出以下研究问题：

### RQ1：开放词汇分割模型能否适应高分辨率遥感场景？

通过 Potsdam 数据集评估 SAM3 对建筑物、道路、树木、低矮植被、车辆等遥感对象的零样本分割能力，分析其优势和不足。

该问题对应当前项目中的 `sam3_potsdam_evaluation.py`。

### RQ2：如何将水利任务转化为 SAM3 可执行的概念提示？

水利问题通常不是单一类别名，而是复合任务。例如：

```text
识别当前影像中新增淹没区，并排除常年水体和阴影区域。
```

该任务需要拆解为多个概念：

```text
current water
permanent water body
flood water
shadow
dark roof
flooded road
flooded farmland
```

然后通过概念组合和逻辑规则得到目标区域。

### RQ3：如何利用水利专业知识修正 SAM3 分割结果？

SAM3 的输出仍可能出现误检和漏检。水利任务中需要引入额外约束：

```text
DEM / DSM 高程约束
坡度约束
河道连通性约束
水系缓冲区约束
历史水体边界约束
水位和降雨过程约束
工程结构拓扑约束
面积、形状和连通域过滤
```

这些约束可以降低阴影、暗色屋顶、湿地、道路等造成的假阳性。

### RQ4：如何构建可解释的水利分割结果？

水利工程应用不能只输出 mask，还需要说明判断依据。例如：

```text
该区域被判定为新增淹没区，因为它在当前影像中被识别为 water，
不属于历史常水体，位于 DEM 低洼区，并与主河道缓冲区连通。
```

因此，本文需要强调输出结果的可解释性和工程决策价值。

## 5. 方法总体框架

建议将论文方法设计为五层架构。

### 5.1 多源数据输入层

水利智能感知不应只依赖 RGB 图像。输入数据可以包括：

```text
遥感 RGB 影像
无人机巡检图像
视频帧
SAR 影像
多光谱 / 高光谱影像
DEM / DSM / nDSM
GIS 水系矢量
河道、堤防、库区边界
历史水体边界
水位、流量、降雨时序
工程设计图纸与巡检记录
用户自然语言问题
```

其中，SAM3 主要处理图像和视频输入；DEM、GIS、水文时序和工程资料主要作为 prompt 生成、结果筛选、空间校正和解释生成的外部知识。

### 5.2 任务理解与概念构造层

该层负责将用户问题转化为可执行的分割任务。

示例：

```text
用户问题：
找出可能由河道漫溢造成的新增积水区。

概念拆解：
1. current water
2. river water
3. flooded farmland
4. flooded road
5. permanent water body
6. shadow
7. dark roof
```

然后结合水利规则构造目标定义：

```text
新增淹没区 =
当前水体
- 历史常水体
- 阴影 / 暗色屋顶等误检区域
+ 与河道、沟渠、低洼区连通的候选水体
```

这一层体现“推理分割”的核心：分割目标不是预先固定类别，而是由任务语义动态生成。

### 5.3 SAM3 概念驱动检测与分割层

SAM3 接收文本概念或视觉提示，输出：

```text
masks
boxes
scores
```

在本文框架中，SAM3 可以承担以下角色：

```text
开放词汇目标发现器
概念级实例分割器
文本提示驱动的候选区域生成器
交互式修正工具
视频目标跟踪器
```

对于静态遥感图像，可使用 SAM3 image model；对于洪水演进、无人机巡检视频、河道动态监测等任务，可扩展到 SAM3 video predictor。

### 5.4 水利知识约束与融合层

SAM3 的原始分割结果需要经过水利知识约束校正。

典型约束包括：

```text
水系连通性约束
DEM / DSM 高程约束
坡度约束
河道缓冲区约束
库区边界约束
历史水体差分
洪水演进时间一致性
工程结构拓扑约束
面积、形状、连通域过滤
```

例如，SAM3 可能将阴影识别为水体。约束层需要判断该区域是否满足：

```text
是否与河道或沟渠连通
是否位于低洼地带
是否符合近期水位和降雨过程
是否属于历史永久水体
是否位于建筑物顶部或高程异常区域
```

这一层是论文区别于普通 SAM3 应用的关键。

### 5.5 结果解释与决策输出层

最终输出不应只有分割图，还应包括：

```text
分割 mask
实例边界框
面积统计
类别或概念置信度
风险等级
水利约束检查结果
可解释文本
工程建议
```

示例输出：

```text
区域 A 被判定为新增淹没区。
理由：
1. SAM3 根据 flood water 概念识别出连续水体 mask；
2. 该区域不属于历史常水位水体；
3. 该区域位于 DEM 低洼区；
4. 该区域与主河道缓冲区连通；
5. 近期水位和降雨过程支持洪水漫溢判断。
```

## 6. 推荐方法流程图

论文中可以画成如下流程：

```text
多源数据输入
遥感影像 / 无人机图像 / 视频 / DEM / GIS / 水文时序 / 用户问题
        ↓
任务理解与概念构造
自然语言问题解析 / 水利目标概念拆解 / prompt 生成
        ↓
SAM3 开放词汇检测与分割
text prompt / visual prompt → boxes / masks / scores
        ↓
水利知识约束与多源融合
高程 / 坡度 / 河网 / 历史水体 / 水位过程 / 连通性校正
        ↓
结果解释与工程输出
分割图 / 面积统计 / 风险等级 / 解释文本 / 工程建议
```

## 7. 当前 Potsdam 实验在论文中的作用

当前项目的核心实验是 SAM3 在 Potsdam 数据集上的零样本评估。需要注意，Potsdam 是城市高分辨率遥感数据集，并非水利专用数据集。因此它在论文中的定位应谨慎。

建议将 Potsdam 实验定位为：

```text
高分辨率遥感场景下 SAM3 开放词汇分割能力的基础验证实验。
```

而不是：

```text
水利场景实验。
```

Potsdam 实验可以回答以下问题：

1. SAM3 是否能在遥感图像中识别建筑物、道路、树木、车辆等常见目标。
2. 文本 prompt 是否可以直接驱动高分辨率遥感地物分割。
3. SAM3 零样本分割与标准语义标签之间存在多大差距。
4. 开放词汇实例分割模型用于语义分割时会遇到哪些问题。
5. 为什么需要后续引入粗分割模型、领域适配或专业知识约束。

### 7.1 当前实验流程

当前脚本 `sam3_potsdam_evaluation.py` 的实验逻辑为：

```text
Potsdam RGB 影像
        ↓
6000×6000 大图切片
        ↓
对每个 patch 输入 6 个文本 prompt
        ↓
SAM3 输出 masks / boxes / scores
        ↓
基于置信度进行多类别 mask 融合
        ↓
合并 patch 得到整图预测
        ↓
与 Potsdam 标签计算 OA、mIoU、mF1、per-class IoU
        ↓
保存预测图、可视化图和指标文件
```

### 7.2 Potsdam 类别与文本 prompt

| 类别 ID | Potsdam 类别 | SAM3 prompt |
| --- | --- | --- |
| 0 | clutter / background | background clutter |
| 1 | impervious surface | impervious surface |
| 2 | building | building |
| 3 | low vegetation | low vegetation |
| 4 | tree | tree |
| 5 | car | car |

建议在论文中说明：背景类不是稳定的开放词汇检测对象，因此可以做两组对比：

```text
方案 A：6 类全部使用 prompt，包括 background clutter
方案 B：只预测 1-5 类前景，未覆盖区域设为 background
```

这样可以体现实验分析的严谨性。

### 7.3 Potsdam 实验的预期结论

Potsdam 实验不一定追求最高 mIoU，而应服务于论文逻辑：

```text
SAM3 具备开放词汇遥感目标发现能力，
但直接用于标准语义分割仍存在类别混淆、背景定义不稳定、重叠 mask 融合困难等问题。
因此，在水利智能感知任务中，需要进一步结合任务语义、专业知识约束和多源数据校正。
```

这可以自然引出本文的“水利知识增强推理分割框架”。

## 8. 后续水利场景实验设计

如果论文需要更强的实证支撑，建议在 Potsdam 之外补充至少一个水利相关实验。

### 8.1 洪水淹没范围提取

目标概念：

```text
flood water
flooded road
flooded farmland
river water
standing water
permanent water body
shadow
dark roof
```

流程：

```text
当前影像
→ SAM3 按 water / flood water 等概念生成候选 mask
→ 与历史水体边界差分
→ 使用 DEM 低洼区和河网连通性约束
→ 输出新增淹没范围、面积和风险对象
```

可对比方法：

```text
UNet / DeepLabV3+ / SegFormer
SAM3 zero-shot
SAM3 + prompt engineering
SAM3 + 水利知识约束
```

评价指标：

```text
IoU
F1
Precision
Recall
淹没面积误差
关键区域漏检率
```

### 8.2 河道、水体与岸线动态识别

目标概念：

```text
main river
tributary
reservoir water
pond
wetland
sandbar
river bank
shoreline
```

重点不是简单提取所有水体，而是区分：

```text
主河道
支流
水库
池塘
湿地
阴影或非目标暗色区域
```

可加入：

```text
历史岸线约束
河网矢量约束
水面面积变化分析
岸线偏移距离
```

### 8.3 堤坝、渠道、闸门等水工结构缺陷识别

目标概念：

```text
dam crack
concrete crack
seepage area
spalling concrete
exposed rebar
slope failure
erosion scar
```

该任务更适合无人机近景图像或巡检图像。SAM3 可用于开放词汇候选缺陷发现，再通过点、框或视觉示例进行交互式细化。

评价指标：

```text
Dice
IoU
缺陷召回率
误报率
边界误差
人工复核评分
```

### 8.4 水库库区与消落带识别

目标概念：

```text
reservoir water surface
drawdown zone
exposed shoreline
mudflat
vegetated bank
```

推理重点：

```text
结合水位数据、库容曲线、DEM 和历史水面线，
判断当前水面边界和消落带范围是否合理。
```

### 8.5 山洪、泥石流、滑坡灾害隐患识别

目标概念：

```text
landslide scar
bare soil
debris flow deposit
blocked river channel
collapsed slope
temporary dam
```

推理重点：

```text
沟道形态
坡度
河网位置
植被破坏
裸露土体
历史灾害记录
```

## 9. 论文创新点设计

### 创新点一：提出水利任务导向的推理分割范式

传统语义分割以固定类别像素分类为目标。本文将水利感知任务建模为由自然语言任务、开放词汇概念和专业知识共同驱动的推理分割问题。

可以表述为：

```text
本文提出面向水利智能感知的推理分割范式，
将分割目标从固定类别像素分类扩展为任务语义驱动的动态概念分割。
```

### 创新点二：构建 SAM3 开放词汇分割与水利知识约束融合框架

本文利用 SAM3 根据文本概念生成候选目标 mask，再结合 DEM、GIS、水系连通性、历史水体和水文时序信息进行校正。

可以表述为：

```text
本文构建了 SAM3 开放词汇分割与水利知识约束相结合的多层框架，
提升了复杂水利场景下目标识别的灵活性和可靠性。
```

### 创新点三：引入概念拆解与多概念组合机制

水利任务往往无法由单一类别表示。本文将复杂任务拆解为多个视觉概念，再通过逻辑组合得到目标区域。

例如：

```text
新增淹没区 = 当前水体 - 历史常水体 - 阴影误检 + 低洼连通区域
```

### 创新点四：强调可解释的工程输出

本文不仅输出 mask，还输出面积统计、风险等级、约束检查结果和自然语言解释，使分割结果更适合水利工程决策。

## 10. 推荐论文结构

### 摘要

摘要应包括：

1. 水利智能感知中分割任务的复杂性。
2. 传统语义分割和 SAM 类方法的不足。
3. 本文提出 SAM3 开放词汇推理分割框架。
4. 引入水利知识约束提升可靠性。
5. 通过 Potsdam 遥感实验和水利场景案例验证方法潜力。

### 第 1 章 绪论

建议逻辑：

```text
水利遥感监测和工程巡检需求增长
→ 传统人工解译效率低
→ 深度学习分割模型发展迅速
→ 固定类别分割难以适应复杂水利任务
→ SAM/SAM2/SAM3 等基础分割模型带来新机会
→ 本文提出水利知识增强的 SAM3 推理分割框架
```

重点说明研究意义：

```text
提高水利灾害识别效率
增强复杂场景目标提取能力
降低新任务标注成本
提升分割结果可解释性
服务洪水监测、河道管理、工程安全巡检等应用
```

### 第 2 章 相关工作

建议分为四类：

1. 遥感图像语义分割方法  
   UNet、DeepLabV3+、SegFormer、Mask2Former 等。

2. 水利遥感监测与灾害识别  
   水体提取、洪水淹没、岸线变化、库区监测、堤坝巡检等。

3. SAM 系列分割基础模型  
   SAM、SAM2、SAM3 的能力差异，重点说明 SAM3 的开放词汇分割特点。

4. 大模型推理与视觉分割融合  
   语言指令分割、视觉语言模型、推理分割、可解释分割等。

### 第 3 章 方法

建议按五层框架展开：

```text
3.1 总体框架
3.2 多源数据输入与预处理
3.3 水利任务理解与概念 prompt 构造
3.4 SAM3 开放词汇检测与分割
3.5 水利知识约束校正
3.6 多类别 / 多概念融合
3.7 可解释结果输出
```

### 第 4 章 实验设计

建议包括两部分。

第一部分：Potsdam 遥感零样本验证。

```text
数据集：ISPRS Potsdam
任务：6 类高分辨率遥感地物分割
目的：验证 SAM3 开放词汇分割在遥感场景中的基础能力
指标：OA、mIoU、mF1、per-class IoU
```

第二部分：水利场景案例或扩展实验。

```text
洪水淹没区 / 水体提取 / 河道岸线 / 堤坝缺陷
对比 SAM3 zero-shot 与 SAM3 + 水利知识约束
指标加入面积误差、连通性一致率、关键目标召回率等工程指标
```

### 第 5 章 结果与分析

分析重点：

```text
SAM3 在遥感目标上的零样本识别能力
不同 prompt 对分割结果的影响
背景类和大面积地物的识别问题
类别混淆问题，例如 tree 与 low vegetation
小目标问题，例如 car
水利知识约束对假阳性和漏检的改善
工程解释结果的有效性
```

### 第 6 章 讨论

建议讨论：

```text
SAM3 用于遥感分割的优势与局限
开放词汇模型与标准语义分割评价之间的差异
水利知识约束的必要性
大模型推理可能带来的幻觉风险
多源数据缺失时的鲁棒性问题
未来微调 SAM3 或构建水利指令分割数据集的方向
```

### 第 7 章 结论

结论应回到主线：

```text
本文将水利智能感知中的分割任务从传统像素分类扩展为推理分割问题，
利用 SAM3 的开放词汇能力实现概念驱动目标发现，
并通过水利知识约束提升结果可靠性和可解释性。
```

## 11. 当前代码与论文的对应关系

| 项目内容 | 论文中对应位置 | 作用 |
| --- | --- | --- |
| `sam3_potsdam_evaluation.py` | 第 4 章实验设计 / 第 5 章结果分析 | SAM3 遥感零样本实验 |
| `config_sam3_evaluation.yaml` | 实验参数设置 | 记录 patch size、stride、prompt、输出路径等 |
| `SAM3_POTSDAM_README.md` | 实验说明 | 描述 Potsdam 实验流程和指标 |
| `Potsdam_Dataset_Documentation.md` | 数据集介绍 | 描述 Potsdam 数据来源、类别和标签 |
| `Untitled 1.md` | 方法框架草稿 | 水利推理分割主线 |
| `简短总结的架构.md` | 旧方案对照 | 说明从 SAM2-LoRA 到 SAM3 的思路转变 |
| `服务器侧确认与处理清单.md` | 实验可靠性检查 | 指出标签格式、logger、融合逻辑等实现风险 |

## 12. 当前实验实现需要修正的问题

在论文实验结果可用之前，当前脚本需要优先确认或修正以下问题。

### 12.1 标签格式问题

Potsdam 标签通常是 RGB 彩色标签，而当前指标计算假设标签是二维类别 ID。

需要实现：

```text
RGB 标签 → 0-5 类别 ID 标签
boundary / ignore → 255
计算指标时排除 ignore 像素
```

否则 mIoU、F1、OA 可能不可信。

### 12.2 logger 初始化顺序问题

当前代码可能在 `self.logger` 创建前调用 `_load_sam3_model()`，需要先初始化日志，再加载模型。

### 12.3 配置文件未读取问题

项目已有 `config_sam3_evaluation.yaml`，但主脚本仍硬编码路径和参数。建议后续加入：

```text
--config config_sam3_evaluation.yaml
```

这样更利于论文实验复现。

### 12.4 patch 融合问题

当前 `merge_patches()` 对类别 ID 做平均，这对离散类别不合理。

更合理的方案：

```text
保存每类置信度图，重叠区域取 argmax
```

或：

```text
对重叠区域做 majority vote
```

### 12.5 背景类 prompt 问题

`background clutter` 不是稳定的开放词汇目标。建议实验中比较：

```text
使用 background prompt
不使用 background prompt，未覆盖区域作为 background
```

### 12.6 SAM3 输出格式兼容问题

需要确认 SAM3 输出的 `masks`、`boxes`、`scores` 是 NumPy 数组还是 torch tensor。如果是 tensor，应先转到 CPU 和 NumPy。

## 13. 论文中可以重点强调的实验分析角度

### 13.1 零样本能力分析

分析 SAM3 是否能直接根据文本 prompt 识别遥感目标。

重点类别：

```text
building
tree
car
impervious surface
low vegetation
```

### 13.2 类别混淆分析

可能出现：

```text
tree 与 low vegetation 混淆
impervious surface 与 building 屋顶混淆
car 小目标漏检
background prompt 误检
```

这些问题可以用于证明“仅靠开放词汇分割不够，需要专业约束和融合策略”。

### 13.3 prompt 消融实验

可以比较不同 prompt：

```text
building / rooftop / building roof
tree / tall tree / trees
low vegetation / grass / lawn
impervious surface / road and pavement / hard surface
car / vehicle
```

评价不同文本概念对遥感分割的影响。

### 13.4 融合策略实验

可以比较：

```text
置信度优先融合
类别优先级融合
前景 residual background 融合
粗分割模型 + SAM3 融合
```

### 13.5 水利知识约束消融

在水利场景实验中，可以比较：

```text
SAM3 only
SAM3 + prompt engineering
SAM3 + DEM
SAM3 + DEM + 河网连通性
SAM3 + DEM + 河网 + 历史水体
```

这能体现专业知识的增益。

## 14. 论文叙述主线

建议全文始终围绕以下主线展开：

```text
水利场景中的目标分割具有强任务语义和强专业约束，
传统固定类别语义分割难以适应开放、复杂、动态的水利任务。

SAM3 提供了开放词汇概念分割能力，
可以根据文本概念直接发现遥感图像中的候选目标。

但 SAM3 的视觉输出仍可能受到阴影、地物相似性、类别边界模糊等因素影响，
因此必须结合水利专业知识进行空间、地形、时序和工程逻辑校正。

最终，本文构建一种面向水利智能感知的推理分割框架，
实现从“看图分割”到“理解任务、发现目标、校正结果、解释原因”的转变。
```

## 15. 一句话总结

本文可以概括为：

```text
本文以 SAM3 开放词汇分割模型为基础，面向水利遥感监测与工程灾害识别任务，
提出一种融合自然语言概念、水利专业知识和多源空间约束的推理分割框架，
并通过高分辨率遥感数据零样本实验验证其基础能力与应用潜力。
```

## 16. 后续工作建议

建议后续按以下顺序推进：

1. 修复 `sam3_potsdam_evaluation.py` 中标签转换、logger 初始化、patch 融合等问题，确保 Potsdam 实验结果可信。
2. 跑通 3 张 Potsdam quick test，获得初步可视化和指标。
3. 增加 prompt 消融实验，分析 SAM3 对不同遥感概念的敏感性。
4. 补充一个真正水利场景案例，例如水体提取或洪水淹没区识别。
5. 在水利案例中加入 DEM、河网或历史水体边界约束，形成“无约束 vs 有约束”的对比。
6. 根据实验结果完善论文中的方法图、表格和消融分析。

## 17. 推荐图表

论文中建议至少准备以下图表：

| 图表 | 内容 | 作用 |
| --- | --- | --- |
| 图 1 | 水利推理分割总体框架图 | 展示本文方法总览 |
| 图 2 | SAM3 概念驱动分割流程图 | 展示 prompt 到 masks 的过程 |
| 图 3 | 水利知识约束校正示意图 | 展示 DEM、河网、历史水体如何修正结果 |
| 图 4 | Potsdam 零样本分割可视化 | 展示 SAM3 基础能力 |
| 图 5 | 水利场景案例可视化 | 展示本文方法应用价值 |
| 表 1 | Potsdam 类别与 prompt 映射 | 说明实验设置 |
| 表 2 | 不同 prompt 的消融结果 | 分析文本提示影响 |
| 表 3 | 不同方法对比结果 | 证明本文方法有效性 |
| 表 4 | 水利约束消融结果 | 证明专业知识约束有效性 |

## 18. 需要避免的写法

论文写作中应避免以下问题：

1. 不要把 Potsdam 直接说成水利数据集。
2. 不要声称 SAM3 能完全解决水利分割问题。
3. 不要只讲大模型概念而没有具体分割流程。
4. 不要只输出 mIoU，而忽略水利工程指标。
5. 不要让大模型直接做水利风险判断，应强调其作为任务理解、提示生成和解释模块。
6. 不要把 SAM2-LoRA 和 SAM3 混成同一技术路线，应明确两者分工和范式差异。

## 19. 最终建议

本文最稳妥的写法是：

```text
以 SAM3 零样本遥感分割实验作为基础验证，
以水利知识增强推理分割框架作为论文创新主体，
以洪水、水体、岸线或堤坝缺陷等场景作为应用扩展，
最终形成“开放词汇分割 + 水利专业约束 + 可解释输出”的完整论文逻辑。
```

这样既能利用当前项目已有代码和 Potsdam 数据，又不会让论文局限在普通城市遥感语义分割上。
