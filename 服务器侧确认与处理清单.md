# 服务器侧确认与处理清单

本文档面向服务器运行环境，用于确认当前 `HydroSegAI` 项目在服务器上执行 SAM3 + Potsdam 零样本评估前需要核实或处理的问题。

已知前提：

- 服务器上的路径 `/home/anjou/PythonENV/Test_11/...` 是正确路径。
- 服务器上的 `sam2/` 和 `sam3/` 目录已具备完整代码、依赖和权重。
- 本文不要求处理本机开发环境中 `sam2/`、`sam3/` 为空或路径不同的问题。

## 1. 必须确认：Potsdam 标签是否为 RGB 彩色标签

当前主脚本 `sam3_potsdam_evaluation.py` 假设标签是二维类别 ID 图：

```python
prediction: (H, W), 取值 0-5
ground_truth: (H, W), 取值 0-5
```

但官方 Potsdam `*_label_noBoundary.tif` 通常是 RGB 彩色标签，例如：

```text
clutter/background       -> [255, 255, 255]
impervious surface       -> [0, 0, 0]
building                 -> [0, 0, 255]
low vegetation           -> [0, 255, 255]
tree                     -> [0, 255, 0]
car                      -> [255, 255, 0]
boundary / ignored area  -> [255, 0, 0]  # 视标签版本而定
```

请服务器侧确认：

```bash
python - <<'PY'
from PIL import Image
import numpy as np

p = "/home/anjou/PythonENV/Test_11/Potsdam/5_Labels_for_participants_no_Boundary/5_Labels_for_participants_no_Boundary/top_potsdam_2_10_label_noBoundary.tif"
a = np.array(Image.open(p))
print(a.shape, a.dtype)

if a.ndim == 3:
    colors = np.unique(a.reshape(-1, a.shape[-1]), axis=0)
else:
    colors = np.unique(a)
print(colors[:20])
print("num unique:", len(colors))
PY
```

如果输出是 `(6000, 6000, 3)`，则必须在评估前将 RGB 标签转换成 0-5 的类别 ID，否则当前指标计算不可信，甚至可能直接报错。

建议处理方式：

- 在 `read_label()` 中读取标签后统一转换为二维 `uint8` 类别图。
- 对 `[255, 0, 0]` 等 ignore/boundary 颜色设置为忽略标签，例如 `255`。
- 在 `calculate_metrics()` 中排除 ignore 像素后再计算 OA、IoU、F1。

建议映射：

```python
COLOR_TO_CLASS = {
    (255, 255, 255): 0,  # clutter/background
    (0, 0, 0): 1,        # impervious surface
    (0, 0, 255): 2,      # building
    (0, 255, 255): 3,    # low vegetation
    (0, 255, 0): 4,      # tree
    (255, 255, 0): 5,    # car
}
IGNORE_INDEX = 255
```

## 2. 必须处理：logger 初始化顺序

当前 `PotsdamSAM3Evaluator.__init__()` 的顺序是：

```python
if SAM3_AVAILABLE:
    self._load_sam3_model()

self._setup_logging()
```

但 `_load_sam3_model()` 内部会调用：

```python
self.logger.info(...)
self.logger.error(...)
```

如果服务器上 SAM3 可以正常导入，程序会先进入 `_load_sam3_model()`，此时 `self.logger` 尚未创建，存在 `AttributeError: 'PotsdamSAM3Evaluator' object has no attribute 'logger'` 风险。

建议改为：

```python
self._setup_logging()

if SAM3_AVAILABLE:
    self._load_sam3_model()
```

这是跨环境必修问题，和服务器路径、权重是否存在无关。

## 3. 建议处理：配置文件未被主脚本实际读取

项目里已有 `config_sam3_evaluation.yaml`，包含路径、测试图像、patch size、stride、阈值、prompt、输出配置等内容。

但 `sam3_potsdam_evaluation.py` 的 `main()` 目前仍硬编码：

```python
BASE_DIR = "/home/anjou/PythonENV/Test_11/Potsdam"
OUTPUT_DIR = "/home/anjou/PythonENV/Test_11/results"
CHECKPOINT_PATH = "/home/anjou/PythonENV/Test_11/sam3/checkpoints/sam3.1_multiplex.pt"

TEST_IMAGES = [
    "top_potsdam_2_10",
    "top_potsdam_5_11",
    "top_potsdam_7_9",
]
```

这不是立即阻断运行的问题，但会导致：

- 修改 YAML 后实际运行参数不变。
- README、YAML、代码三处配置容易漂移。
- 后续 quick/full evaluation、prompt variants、threshold sweep 不方便复现实验。

建议服务器侧确认是否需要让主脚本读取 YAML。如果需要，建议：

- 新增 `--config config_sam3_evaluation.yaml` 参数。
- 默认仍使用当前服务器路径，保持兼容。
- 将 `PATCH_SIZE`、`STRIDE`、`SCORE_THRESHOLD` 从类常量改为实例配置。

## 4. 建议确认：SAM3 输出字段和张量格式

当前 `predict_patch()` 假设：

```python
output = self.processor.set_text_prompt(...)
masks = output.get("masks", [])
boxes = output.get("boxes", [])
scores = output.get("scores", [])
```

并且后续直接执行：

```python
valid_indices = np.array(scores) >= self.SCORE_THRESHOLD
mask.shape
mask.astype(np.uint8)
```

请服务器侧确认 SAM3 当前版本的 `Sam3Processor.set_text_prompt()` 返回字段是否确实是：

- `masks`
- `boxes`
- `scores`

并确认这些字段类型是否可能是 `torch.Tensor`。如果是 tensor，应在进入 NumPy/OpenCV 逻辑前统一转换：

```python
if isinstance(mask, torch.Tensor):
    mask = mask.detach().cpu().numpy()

if isinstance(scores, torch.Tensor):
    scores = scores.detach().cpu().numpy()
```

否则可能在 GPU tensor、batch 维度、布尔索引等位置出现运行错误。

## 5. 建议确认：背景类是否适合用 SAM3 文本提示直接预测

当前 6 类全部作为文本 prompt 送入 SAM3：

```python
background clutter
impervious surface
building
low vegetation
tree
car
```

需要确认 `background clutter` 是否真的适合作为 SAM3 文本检测/分割目标。

在开放词汇分割中，背景类通常不是一个稳定的可检测目标。更稳妥的策略可能是：

- 只预测 1-5 类前景对象或地物。
- 最终未被任何前景 mask 覆盖的区域赋值为 0，即 clutter/background。

如果服务器侧坚持直接评估 6 个 prompt，也建议至少单独记录：

- 是否 background prompt 产生大量误检。
- 不使用 background prompt 时，背景作为 residual class 的指标变化。

## 6. 建议确认：patch 融合逻辑是否适合类别标签

当前 `merge_patches()` 对重叠 patch 使用数值平均：

```python
merged[y:y+patch_h, x:x+patch_w] += patch
count_map[y:y+patch_h, x:x+patch_w] += 1
merged = merged / count_map
return merged.astype(patches[0][0].dtype)
```

这对连续概率图合理，但对离散类别 ID 不严谨。例如一个像素在两个 patch 中分别预测为类别 1 和 5，平均得到 3，最终会被解释成 `low vegetation`，这是错误类别。

建议处理方式：

- 保存每个类别的置信度图，再对重叠区域取 `argmax`。
- 或至少对重叠区域做 majority vote。
- 如果当前仅做快速 baseline，也需要在结果说明中标注该融合方式会引入类别平均误差。

## 7. 建议确认：类别颜色与 Potsdam 官方定义是否完全一致

当前代码中类别颜色为：

```python
0: [255, 255, 255]
1: [0, 0, 0]
2: [0, 0, 255]
3: [0, 255, 255]
4: [0, 255, 0]
5: [255, 255, 0]
```

请服务器侧用实际标签文件确认颜色与类别含义一致，尤其注意：

- PIL 读取顺序是 RGB。
- OpenCV 默认读取顺序是 BGR，但当前脚本主要用 PIL/GDAL 读取，理论上按 RGB 处理。
- 如果 GDAL 读取多波段标签，数组可能是 `(C, H, W)`，需要转为 `(H, W, C)` 后再做颜色映射。

## 8. 建议的最小修复顺序

建议服务器侧按以下顺序处理：

1. 修复 logger 初始化顺序。
2. 确认标签格式，并实现 RGB label -> class ID label。
3. 在指标计算中排除 ignore/boundary 像素。
4. 对 SAM3 输出 tensor/NumPy 格式做兼容转换。
5. 修正重叠 patch 的类别融合方式。
6. 再考虑读取 YAML 配置和扩展 full evaluation。

## 9. 建议的服务器侧验收方式

建议先不要直接跑完整 quick 三张大图，而是增加一个小规模 smoke test：

- 读取一张图和标签。
- 只裁剪左上角或中心区域一个 patch。
- 运行一个类别 prompt，例如 `building`。
- 检查输出 mask shape、score 类型、预测类别图 shape。
- 确认 `calculate_metrics()` 可以正常输出非异常指标。

通过 smoke test 后，再运行：

```bash
python sam3_potsdam_evaluation.py
```

最后检查输出：

```text
results/
├── predictions/
├── visualizations/
├── metrics/
└── logs/
```

重点查看：

- `metrics/overall_metrics.json`
- `metrics/per_image_metrics.csv`
- 每张图的 comparison visualization
- 日志中是否有某些类别 prompt 大量失败

## 10. 当前结论

服务器路径和模型目录不需要调整；真正需要服务器侧确认/处理的是标签格式、logger 初始化、SAM3 输出兼容、离散类别融合和配置读取。

其中标签 RGB 转类别 ID 与 logger 初始化是最优先问题。若不处理标签格式，评估指标基本不能作为论文或实验 baseline 使用。
