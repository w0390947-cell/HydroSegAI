# 遥感推理分割技术体系方法框架

## 1. 方法目标

本文最终目标不是提出一个只适用于 Potsdam 的模型，而是构建一套可迁移的遥感推理分割技术体系。

该体系面向的问题是：

```text
给定一幅高分辨率遥感图像和任务目标，
如何结合 coarse semantic model、open-vocabulary proposal model、
不确定性分析、空间先验和解释机制，
输出稳定语义图、风险区域、候选对象和可解释结果。
```

## 2. 总体框架

框架可以概括为：

```text
Remote-Sensing Reasoning Segmentation Framework

Input image
  ↓
Coarse Semantic Segmenter
  ↓
Dense semantic prediction + logits / confidence
  ↓
Uncertainty & Candidate Region Generator
  ↓
Candidate region / review priority map
  ↓
Open-Vocabulary Proposal Generator
  ↓
Object proposals / risk proposals
  ↓
Proposal Quality Auditor
  ↓
Reasoning Output Packager
  ↓
Final semantic map + review map + proposals + explanation manifest
```

## 3. 模块一：Coarse Semantic Segmenter

作用：

```text
提供稳定的 dense semantic prediction。
```

当前实现：

```text
SegFormer-B0 supervised baseline
```

可替换为：

```text
DeepLabV3+
U-Net
Swin-UNet
Mask2Former
其他遥感语义分割模型
```

输出：

```text
prediction_id
colored_prediction
logits / confidence
per-class metrics if GT available
```

在最终体系中，它是主干模块，而不是被 SAM3 替代的对象。

## 4. 模块二：Uncertainty & Candidate Region Generator

作用：

```text
定位 coarse prediction 中最值得复核、解释或局部处理的区域。
```

当前使用：

```text
confidence
entropy
margin
boundary band
```

候选区域生成：

```text
candidate_region =
  high_entropy
  OR low_margin
  OR selected_boundary_band
```

输出：

```text
candidate mask
high entropy mask
low margin mask
boundary band mask
review priority map
```

该模块是可迁移的，因为它只依赖 coarse model 的输出和空间边界，不依赖 Potsdam 特定标签。

## 5. 模块三：Open-Vocabulary Proposal Generator

作用：

```text
根据任务概念或类别 prompt，在 candidate patch 中生成 candidate masks。
```

当前实现：

```text
SAM3 prompt ensemble
```

可替换为：

```text
SAM / SAM2 / SAM3
Grounded-SAM
开放词汇实例分割模型
遥感基础分割模型
```

输出：

```text
mask
score
class prompt
box or region metadata
```

注意：

```text
该模块不直接决定最终语义标签。
```

它只生成候选对象和候选区域。

## 6. 模块四：Proposal Quality Auditor

作用：

```text
判断 proposal 应承担什么角色。
```

当前分层：

```text
object_proposal_high_precision
object_proposal_medium_precision
error_region_explanation
large_surface_risky
low_confidence_or_ambiguous
empty_or_unusable
```

输出建议：

```text
candidate_object_layer
risk_explanation_layer
interactive_refinement_proposal
do_not_auto_refine
needs_additional_prior
```

该模块避免了一个关键风险：

```text
不把开放词汇模型的 proposal 直接当成最终语义分类结果。
```

## 7. 模块五：Reasoning Output Packager

作用：

```text
将所有模块结果组织为可解释输出包。
```

输出结构：

```text
final_predictions/
review_priority/
candidate_components/
object_proposals/
overlays/
manifests/
metrics/
```

当前 Exp5-F 已实现：

```text
final semantic prediction
review priority map
candidate components
priority overlay
per-image manifest
package manifest
```

该输出包比单一 mask 更适合实际遥感应用，因为它提供：

```text
结果；
风险；
解释；
候选；
复核优先级。
```

## 8. 为什么不是硬融合

实验结果表明：

```text
SAM3 candidate masks 自动写入语义图会带来较高误伤。
```

Exp5-D 中：

```text
improved pixels: 810
degraded pixels: 4,928
change precision: 0.1262
degradation rate: 0.7676
```

因此最终体系不采用：

```text
SAM3 mask 覆盖 coarse prediction。
```

而采用：

```text
coarse prediction as final semantic base
+ review priority
+ candidate object proposal
+ risk explanation
+ optional human / prior-guided refinement
```

这是当前最稳妥、最可迁移的设计。

## 9. 面向水利场景的迁移方式

迁移到水利场景时，各模块对应为：

```text
Coarse Semantic Segmenter:
  水体 / 建筑 / 道路 / 植被 / 裸地等基础地物分割。

Uncertainty & Candidate Region:
  水陆边界、低置信水体、疑似洪水边缘、河道附近混淆区。

Open-Vocabulary Proposal:
  water, river, canal, flooded area, levee, dam, sluice, embankment 等 prompt。

Spatial Prior:
  DEM、河网、历史水体、流域边界、工程设施位置。

Output Packager:
  水体分割图 + 洪水风险复核图 + 候选水工设施 proposal + 解释 manifest。
```

这样就能从 Potsdam 的通用遥感验证过渡到水利任务。

## 10. 可写入论文的方法表述

方法可以表述为：

```text
本文提出一种面向遥感场景的推理分割框架。
该框架首先利用监督语义分割模型生成稳定的 dense prediction，
再基于 confidence、entropy、margin 和边界信息构造候选复核区域；
随后利用开放词汇分割模型在候选区域内生成对象级 proposals，
并通过 proposal quality audit 将其划分为对象候选、错误解释和风险提示；
最终输出语义分割图、复核优先级图、候选 proposal 和解释 manifest，
从而避免开放词汇模型直接 zero-shot 或直接硬融合带来的不稳定问题。
```

## 11. 当前阶段结论

当前方法框架已经由 Exp1-Exp5 支撑：

```text
Exp1-Exp3:
  证明 SAM3 直接 zero-shot / prompt dense segmentation 不足。

Exp4:
  证明 supervised coarse semantic model 是稳定主干。

Exp5:
  证明 uncertainty candidate region 有效；
  证明 SAM3 自动 refinement 风险较高；
  证明 SAM3 proposal 更适合作为候选对象和解释层；
  形成推理分割输出包。
```

因此，当前最合理的论文路线是：

```text
从“开放词汇模型直接迁移失败”
走向“粗语义模型 + 开放词汇 proposal + 风险解释 + 输出包”的推理分割体系。
```
