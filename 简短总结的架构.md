该文章使用的模型架构可以总结为：

**“语义粗分割模型 + SAM2-LoRA 边界细化模型 + 多类别融合模块”**。

核心不是让 SAM2 直接完成 6 类语义分割，而是让不同模块分工协作。

## 1. 总体架构

```text
Potsdam 遥感影像
        ↓
数据预处理与切片
        ↓
粗语义分割分支
        ↓
类别候选区域 / prompt 生成
        ↓
SAM2-LoRA 精细分割分支
        ↓
多类别 mask 融合
        ↓
最终 6 类语义分割结果
```

## 2. 输入层

输入为 Potsdam 高分辨率遥感影像，主要采用：

```text
RGB 正射影像
```

可扩展使用：

```text
IRRG 影像
RGBIR 影像
DSM / nDSM 高度信息
```

由于 SAM2 默认适配 3 通道输入，文章第一版建议以 RGB 或 IRRG 作为主输入，DSM 更适合作为后处理或辅助先验。

## 3. 粗语义分割分支

该分支可以使用：

```text
SegFormer / DeepLabV3+ / U-Net
```

作用是先判断每个像素大致属于哪一类：

```text
clutter/background
impervious surface
building
low vegetation
tree
car
```

粗分割分支负责回答：

```text
“这个区域大概是什么类别？”
```

它输出的是 6 类 coarse semantic map。

## 4. Prompt 生成模块

根据粗分割结果，对每个类别提取连通区域，然后自动生成 SAM2 所需的 prompt。

常用 prompt 包括：

```text
box prompt
point prompt
box + point prompt
coarse mask prompt
```

例如：

```text
粗分割得到 building 区域
        ↓
提取 building 连通域
        ↓
生成 bounding box
        ↓
送入 SAM2-LoRA 细化建筑物边界
```

该模块是连接“语义分割模型”和“SAM2”的关键桥梁。

## 5. SAM2-LoRA 精细分割分支

SAM2 负责根据 prompt 输出高质量 mask。

LoRA 微调方式为：

```text
冻结 SAM2 主体参数
仅在 image encoder 的 attention projection 层中注入 LoRA
```

推荐注入位置：

```text
q_proj
v_proj
```

可扩展为：

```text
q_proj + k_proj + v_proj
q_proj + k_proj + v_proj + out_proj
```

这一分支负责回答：

```text
“这个目标的精确边界在哪里？”
```

## 6. 类别级二值训练机制

由于 SAM2 不是天然的多类别语义分割模型，因此训练时将 Potsdam 的 6 类任务拆成多个二值分割任务。

例如：

```text
building vs others
tree vs others
car vs others
impervious surface vs others
low vegetation vs others
clutter/background vs others
```

训练样本形式为：

```text
image patch + target class + binary mask + box prompt
```

也就是说，SAM2-LoRA 学习的是：

```text
在给定类别提示和区域提示的情况下，生成该类别的精细 mask。
```

## 7. 多类别融合模块

推理阶段，每个类别都会得到一个候选 mask。由于不同类别 mask 可能重叠，因此需要融合。

融合依据可以包括：

```text
粗分割类别概率
SAM2 mask score
类别优先级
连通域大小
DSM / nDSM 高度先验
```

最终得到一张完整的 6 类语义分割图。

## 8. 架构核心思想

该模型架构的本质是：

```text
粗分割模型负责语义类别判断；
SAM2-LoRA 负责目标边界细化；
LoRA 负责让 SAM2 适应 Potsdam 遥感图像域；
融合模块负责生成最终多类别语义分割结果。
```

## 9. 一句话总结

本文模型架构可以概括为：

**以 SegFormer 等语义分割模型生成类别候选区域，以 SAM2-LoRA 作为遥感域适配后的精细 mask 生成器，通过 prompt 生成与多类别融合，实现 Potsdam 高分辨率遥感影像的精细语义分割。**
