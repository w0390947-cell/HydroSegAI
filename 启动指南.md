# SAM3 Potsdam 评估启动指南

## ✅ 环境准备完成

所有依赖包已成功安装，SAM3 模块已正确配置！

### 📦 已安装的关键包
- ✅ **SAM3** - 分割模型 (v0.1.0)
- ✅ **PyTorch** - 深度学习框架 (支持CUDA，但会回退到CPU)
- ✅ **NumPy** - 数值计算 (v1.26.4)
- ✅ **OpenCV** - 图像处理 (v4.13.0.92)
- ✅ **matplotlib** - 可视化 (v3.10.9)
- ✅ **pandas** - 数据处理 (v3.0.3)
- ✅ **scikit-learn** - 评估指标 (v1.8.0)

### 🚀 快速启动

#### 方法 1: 使用默认配置
```bash
cd /home/anjou/PythonENV/Test_11
source .venv_hf/bin/activate
python sam3_potsdam_evaluation.py
```

#### 方法 2: 使用自定义配置
```bash
cd /home/anjou/PythonENV/Test_11
source .venv_hf/bin/activate
python sam3_potsdam_evaluation.py --config your_config.yaml
```

#### 方法 3: 直接使用虚拟环境Python
```bash
cd /home/anjou/PythonENV/Test_11
./.venv_hf/bin/python sam3_potsdam_evaluation.py
```

## ⚙️ 运行时注意事项

### 1. CUDA 警告
**警告信息**: `The NVIDIA driver on your system is too old`
**影响**: 程序会自动使用CPU运行，速度较慢但功能正常
**解决方案**: 如需GPU加速，请更新NVIDIA驱动

### 2. GDAL 警告
**警告信息**: `GDAL 未安装，将尝试使用 PIL 读取图像`
**影响**: 使用PIL读取GeoTIFF文件，功能正常
**解决方案**: 可选安装GDAL以获得更好的地理数据处理性能

### 3. 设备选择
**当前**: CPU (因驱动版本限制)
**建议**: 对于生产环境，建议更新GPU驱动以使用CUDA加速

## 📊 评估配置

### 默认测试图像
```python
TEST_IMAGES = [
    "top_potsdam_2_10",  # 简单城市场景
    "top_potsdam_5_11",  # 中等复杂度场景
    "top_potsdam_7_9",   # 复杂密集建筑场景
]
```

### 默认处理参数
- **Patch Size**: 1008 像素
- **Stride**: 672 像素
- **Score Threshold**: 0.5
- **类别数**: 6 类

## 📂 输出结果

运行完成后，结果将保存在：
```
/home/anjou/PythonENV/Test_11/results/
├── predictions/           # 预测标签
├── visualizations/        # 可视化对比图
├── metrics/              # 评估指标
│   ├── overall_metrics.json
│   └── per_image_metrics.csv
└── logs/                 # 运行日志
```

## 🧪 验证安装

### 快速测试
```bash
source .venv_hf/bin/activate
python -c "from sam3.model_builder import build_sam3_image_model; print('✅ SAM3 OK')"
```

### 运行修复验证
```bash
source .venv_hf/bin/activate
python test_fixes.py
```

## 🐛 常见问题

### Q: 程序运行很慢怎么办？
A: 当前使用CPU运行，可以考虑：
1. 更新NVIDIA驱动以启用GPU加速
2. 减少测试图像数量
3. 增大stride参数减少重叠区域

### Q: 内存不足怎么办？
A: 可以调整参数：
- 减小 `PATCH_SIZE`
- 增大 `STRIDE` 减少重叠
- 处理更小的图像区域

### Q: 如何修改评估参数？
A: 编辑配置文件 `config_sam3_evaluation.yaml` 或修改代码中的默认值

## 📝 相关文档

- **详细修复报告**: `服务器侧处理完成报告.md`
- **问题处理清单**: `工程师侧问题处理与确认清单.md`
- **虚拟环境说明**: `虚拟环境使用说明.md`

## 🎯 预期运行时间

- **单张图** (CPU): 约 15-30 分钟
- **三张图** (默认): 约 45-90 分钟
- **完整数据集**: 需要数小时

*注: 时间取决于硬件配置和参数设置*

---

**状态**: ✅ 就绪
**最后更新**: 2026-05-15
**下一步**: 运行评估脚本开始实验