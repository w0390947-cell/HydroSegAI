# 高精度语义底图增强机制说明

## 1. 问题背景

在当前遥感推理分割框架中，SegFormer 的作用是生成一张稳定的像素级语义底图。

它输出的是：

```text
每个像素属于哪个语义类别
+ 每个像素属于各类别的概率
+ 由概率计算得到的不确定性信息
```

在 Potsdam 阶段，SegFormer 当前预测的语义类别包括：

```text
impervious surface
building
low vegetation
tree
car
clutter/background
```

如果后续迁移到水利场景，类别体系可以扩展为：

```text
water
river
flooded area
dam
embankment
road
building
vegetation
bare soil
vehicle
ship
floating object
hydraulic facility
```

因此，SegFormer 或其他监督语义分割模型在框架中的定位是：

```text
提供高精度、连续、稳定的遥感场景语义底图。
```

这张语义底图是后续自然语言推理分割、候选 mask 生成、空间关系判断和目标筛选的基础。

## 2. 高质量训练能否消除不确定性

高质量数据集训练后的 SegFormer，确实可以显著提升像素级语义图的稳定性和准确率。

在训练数据足够充分、标注质量足够高、类别定义足够清楚的情况下，模型在常见区域上可以表现得非常稳定。例如：

```text
大面积道路
清晰建筑屋顶
明显植被
规则水体
大型裸土区域
```

这些区域通常会具有：

```text
高 confidence
低 entropy
高 margin
```

也就是说，它们看起来几乎没有不确定性。

但是，不确定性不太可能被完全消除。遥感与水利场景中长期存在以下困难区域：

```text
类别边界：
建筑边缘、道路边缘、水陆交界、岸线区域。

混合像素：
一个像素或一个局部区域同时包含水体、植被、阴影、道路或泥沙。

小目标：
车辆、船只、漂浮物、闸门小结构、临时设施。

遮挡与阴影：
树影、建筑阴影、水面反光、云影。

语义模糊区：
湿地、积水道路、裸土、施工区、淤泥、浅水区。

分布外场景：
新地区、新季节、新水位、新传感器、新拍摄条件。

标注本身不确定：
不同标注者对边界或模糊区域可能给出不同判断。
```

此外还需要注意：

```text
模型高置信不等于一定正确。
```

模型可能在某些区域非常自信地预测错误。这类错误尤其危险，因为它们不一定能被普通的低置信度机制捕获。

因此，更合理的目标不是训练一个完全没有不确定性的 SegFormer，而是：

```text
训练一个足够强的主语义模型，
并额外保留不确定性定位、边界细化、局部复核和质量审计机制。
```

## 3. 高精度语义底图的总体路线

如果最终目标是获得一张尽可能准确的像素级语义底图，那么不应只依赖单个 SegFormer。

更合理的路线是：

```text
高质量标注数据
    ↓
强监督语义分割模型
    ↓
多源遥感信息增强
    ↓
不确定性 / 边界 / 小目标检测
    ↓
局部细化与质量审计
    ↓
高精度语义底图
```

可以将最终语义底图生成模块设计为：

```text
Strong Coarse Semantic Segmenter
+ Multi-source Remote Sensing Priors
+ Boundary Refinement
+ Small Object Module
+ Uncertainty-based Local Review
+ Quality Audit
```

也就是说，SegFormer 是主干，但不是唯一模块。

## 4. 机制一：使用更强的监督语义分割模型

当前 SegFormer-B0 是一个合理的轻量基线。

如果追求更高精度，可以进一步尝试：

```text
SegFormer-B2 / B4 / B5
Mask2Former
UPerNet + Swin Transformer
DeepLabV3+
HRNet
UNetFormer
遥感专用 Transformer 分割模型
```

不同模型的优势不同：

```text
SegFormer：
结构简洁，训练和推理相对稳定，适合作为强基线。

Mask2Former：
对区域级 mask 建模更强，适合复杂语义区域。

HRNet：
高分辨率特征保持较好，适合边界和小目标。

DeepLabV3+：
经典强基线，对多尺度上下文较稳。

遥感专用模型：
通常更适合高分辨率遥感纹理、尺度变化和地物结构。
```

如果条件允许，还可以做多模型 ensemble：

```text
SegFormer + Mask2Former + DeepLabV3+
```

多模型融合的价值在于：

```text
不同模型的错误模式不完全相同；
融合后可以降低单模型系统性误差；
对边界、小目标、局部纹理混淆区域通常更稳。
```

## 5. 机制二：引入多源遥感数据

只使用 RGB 时，很多遥感类别天然难以区分。

在遥感与水利场景中，应尽量引入多源数据：

```text
RGB
NIR / 多光谱
DSM / DEM / nDSM
NDVI
NDWI / MNDWI
坡度、高程
河网矢量
历史水体范围
水利工程边界
降雨、水位等水文信息
```

这些信息对不同类别有不同价值：

```text
水体：
NDWI / MNDWI、历史水体范围、河网、水位信息很有帮助。

植被：
NDVI、NIR、多光谱特征很有帮助。

建筑：
DSM / nDSM、高程突变、规则边界很有帮助。

道路和硬化地表：
RGB 纹理、DSM 平坦性、连通结构、交通网络先验很有帮助。

积水道路或淹没区：
NDWI、水体先验、道路先验、低洼地形共同判断更可靠。

船只、车辆、漂浮物：
高分辨率 RGB、小目标检测、实例候选和场景关系都很重要。
```

需要特别注意：

```text
多源遥感信息不一定适合直接塞给 SAM3。
```

前面 Exp3 已经说明，IRRG / RGBIR 派生三通道直接替代 RGB 输入 SAM3，并不会自动带来提升，反而可能破坏 SAM3 原有的自然图像视觉对齐。

更合理的方式是：

```text
将多源信息用于监督语义模型训练；
或作为空间先验、候选筛选、质量审计和规则判断信号。
```

## 6. 机制三：Coarse-to-Fine 局部细化

高精度底图不一定要整图反复重分割。

更合理的方式是：

```text
先由主模型生成整图语义底图；
再只对高风险区域进行局部细化。
```

高风险区域可以包括：

```text
低 confidence 区域
高 entropy 区域
低 margin 区域
类别边界区域
小目标密集区域
水陆交界区域
历史错误高发区域
空间先验冲突区域
```

局部细化模块可以包括：

```text
更高分辨率 patch 模型
边界细化模型
SAM3 / SAM2 proposal
局部专用分类器
规则约束后处理
人工复核接口
```

其基本思想是：

```text
全图由强监督模型保证稳定性；
局部由高分辨率和候选机制处理细节。
```

这比让 SAM3 直接覆盖全图语义图更安全，也更符合当前 Exp5 的实验结论。

## 7. 机制四：边界 refinement

像素级语义底图最容易错在边界。

典型边界包括：

```text
建筑边缘
道路边缘
水陆交界
植被与道路交界
阴影与真实地物交界
裸土与浅水交界
```

可以加入边界相关机制：

```text
boundary loss
edge-aware loss
边界专用预测分支
CRF / graph cut 后处理
morphological refinement
高分辨率局部裁剪 refinement
基于 SAM3 / SAM2 的边界候选 mask
```

边界模块的作用不是替代主语义模型，而是改善主模型容易模糊的边缘区域。

在当前项目中，Exp5-B 已经实现了第一版 boundary band：

```text
从 SegFormer prediction 中提取指定类别边界；
通过 morphology gradient 和 dilation 得到边界带；
将边界带并入 candidate region。
```

后续可以进一步做边界消融实验：

```text
只用 uncertainty
只用 boundary
uncertainty + boundary
不同 boundary radius
不同 boundary class set
边界区域上的局部 refinement 是否有效
```

## 8. 机制五：小目标专门处理

遥感图像中的小目标很容易被普通语义分割模型漏掉或粘连。

典型小目标包括：

```text
车辆
船只
漂浮物
闸门小结构
临时施工设施
水面障碍物
```

可以增加小目标专门模块：

```text
小目标检测器
实例分割模型
SAM3 candidate proposal
连通域筛选
面积 / 长宽比 / 形状规则
局部超分辨率或高分辨率 patch 推理
```

在当前实验中，Exp5-E 已经提供了一个重要线索：

```text
SAM3 的大面积 surface masks 语义精度较低；
但大量 car-like 小面积 masks 的精度较高。
```

这说明 SAM3 在体系中的一个重要角色是：

```text
小目标 object proposal 模块。
```

但这些 proposal 不应直接自动写回语义图，而应经过：

```text
语义一致性检查
面积和形状检查
场景先验检查
候选排序
质量审计
```

## 9. 机制六：主动学习与错误回流

如果目标是持续提高语义底图质量，主动学习非常关键。

模型不确定的区域，恰好是最值得继续标注的区域。

可以形成如下闭环：

```text
模型预测
    ↓
发现高不确定 / 高错误风险区域
    ↓
人工少量精标
    ↓
加入训练集
    ↓
重新训练或微调
    ↓
语义底图质量提升
```

这种方式比盲目增加训练数据更高效，因为新增标注会集中打中模型短板。

主动学习可以优先选择：

```text
高 entropy 区域
低 margin 区域
模型间分歧区域
边界模糊区域
小目标漏检区域
水陆交界区域
空间先验冲突区域
人工复核频繁修改区域
```

对水利场景尤其重要的是：

```text
不同地区、季节、水位和灾害状态下的样本需要持续补充。
```

## 10. 机制七：质量审计与风险图

即使最终语义底图已经很准，也不应只输出一张颜色图。

更完整的高精度底图产品应包含：

```text
final semantic map
confidence map
entropy map
margin map
boundary risk map
review priority map
candidate proposal layer
quality audit manifest
```

这样系统不仅告诉用户：

```text
每个像素属于哪个类别。
```

还告诉用户：

```text
哪些地方可靠；
哪些地方不可靠；
哪些区域建议复核；
哪些候选 mask 支持或质疑当前预测。
```

这对于水利场景尤其重要，因为很多应用并不只是追求平均指标，而是要求知道风险区域在哪里。

## 11. 与自然语言推理分割框架的关系

高精度语义底图是自然语言推理分割的基础，但不是最终目标的全部。

可以将整个体系分为两层：

```text
第一层：高精度语义底图生成
由 SegFormer / Mask2Former / 多源监督模型 / 局部细化模块负责。

第二层：自然语言推理目标分割
由大模型 query parser、SAM3 proposal、候选排序和空间关系判断负责。
```

第一层回答：

```text
整张图中每个像素大致属于什么语义类别？
哪里是道路、水体、建筑、植被、车辆、工程设施？
哪些地方可能存在预测风险？
```

第二层回答：

```text
用户 query 描述的具体目标是哪一个？
哪个候选 mask 最符合目标类别、属性和空间关系？
最终应该输出哪个目标 mask？
```

例如用户输入：

```text
建筑旁边的白色小汽车
```

第一层提供：

```text
building 区域
car 区域
road / impervious surface 区域
不确定区域
边界风险区域
```

第二层负责：

```text
解析 white car near building；
生成 car candidate masks；
判断候选颜色是否偏白；
计算候选与 building 的距离；
选择最符合 query 的目标 mask。
```

因此，高精度语义底图越准，后续自然语言推理分割越稳。

但即使语义底图很强，仍然需要 query 解析、候选 mask、空间关系判断和候选排序，因为用户最终问的往往不是“哪里是某一类”，而是：

```text
哪一个目标符合这句话描述的条件？
```

## 12. 建议的系统结构

可以将最终框架中的高精度底图模块设计为：

```text
Input remote sensing data
    ↓
Multi-source preprocessing
    ↓
Strong semantic segmentation model
    ↓
Dense semantic prediction + logits
    ↓
Uncertainty and boundary analysis
    ↓
Local refinement / small object module
    ↓
Quality audit
    ↓
High-precision semantic base map
```

然后再进入自然语言推理分割模块：

```text
Natural language query
    ↓
LLM / rule-based query parser
    ↓
Target / attribute / relation constraints
    ↓
Candidate mask proposal
    ↓
Candidate feature extraction
    ↓
Candidate ranking
    ↓
Selected target mask + explanation
```

## 13. 一句话总结

如果最终目标是得到一张非常准确的像素级语义底图，那么不应只依赖单个 SegFormer。

更合理的方向是：

```text
强监督语义模型
+ 多源遥感先验
+ 不确定性定位
+ 边界细化
+ 小目标模块
+ 局部 refinement
+ 主动学习
+ 质量审计
```

共同生成高精度语义底图。

在这个基础上，再接入自然语言 query 解析、SAM3 候选 mask、空间关系判断和候选排序，才能形成完整的遥感 / 水利自然语言推理分割框架。
