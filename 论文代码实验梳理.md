# 论文代码实验梳理：SAM3 开放词汇遥感分割与水利推理分割实验方案

## 1. 文档目的

本文档用于梳理论文所需的代码实验体系，重点回答以下问题：

```text
论文需要哪些实验？
当前项目代码已经支持哪些实验？
哪些实验还需要补充代码？
每个实验需要什么数据、输入、输出和指标？
如何组织实验结果，才能支撑论文写作？
```

本文档与 `论文逻辑梳理.md` 的关系如下：

```text
论文逻辑梳理.md
→ 解决论文讲什么、创新点是什么、章节如何组织。

论文代码实验梳理.md
→ 解决实验怎么做、代码怎么写、结果怎么保存、表格怎么生成。
```

## 2. 总体实验目标

本文论文的实验不应只围绕“跑通 SAM3”展开，而应服务于论文主线：

```text
SAM3 具备开放词汇遥感目标发现能力；
但直接用于标准语义分割仍存在局限；
结合 prompt 工程、融合策略和水利专业知识约束后，可以提升复杂场景分割的可靠性和可解释性。
```

因此，实验体系建议分为三层：

```text
第一层：SAM3 在高分辨率遥感图像上的零样本能力验证
第二层：SAM3 遥感分割改进实验，包括 prompt、融合、背景处理等
第三层：水利场景推理分割实验，包括水体、洪水、河道、库区或工程缺陷
```

对应论文中的作用：

| 实验层级 | 实验目的 | 论文作用 |
| --- | --- | --- |
| Potsdam 零样本实验 | 验证 SAM3 对遥感对象的基础识别能力 | 基础能力验证 |
| Potsdam 改进实验 | 分析 prompt、融合策略、背景处理对结果的影响 | 方法分析与消融 |
| 水利场景实验 | 验证水利知识约束和推理分割框架的价值 | 论文核心应用支撑 |

## 3. 当前项目代码基础

当前项目根目录下与实验相关的主要文件为：

| 文件 | 当前作用 | 后续建议 |
| --- | --- | --- |
| `sam3_potsdam_evaluation.py` | SAM3 在 Potsdam 上的零样本评估主脚本 | 需要修复标签、融合、配置读取等问题 |
| `config_sam3_evaluation.yaml` | SAM3 评估参数配置 | 需要真正接入主脚本 |
| `SAM3_POTSDAM_README.md` | Potsdam 零样本实验说明 | 可作为实验复现说明基础 |
| `Potsdam/Potsdam_Dataset_Documentation.md` | Potsdam 数据集说明 | 可用于论文数据集介绍 |
| `服务器侧确认与处理清单.md` | 服务器运行前风险清单 | 可转化为代码修复任务列表 |
| `论文逻辑梳理.md` | 论文整体逻辑文档 | 与本文档配合使用 |

当前已有代码主要支持：

```text
Potsdam RGB 图像读取
Potsdam 图像切片
SAM3 文本 prompt 推理
多类别 mask 融合
整图预测结果保存
OA、mIoU、F1 等指标计算
可视化对比图生成
```

但目前尚不完整或不可靠的部分包括：

```text
Potsdam RGB 彩色标签转类别 ID
ignore / boundary 像素处理
logger 初始化顺序
YAML 配置读取
SAM3 tensor 输出兼容
重叠 patch 的离散类别融合
背景类 residual 处理
prompt 消融实验自动化
多次实验结果汇总
水利场景知识约束代码
```

## 4. 建议实验目录结构

为了后续论文结果可复现，建议将结果目录规范化。

推荐结构：

```text
results/
├── potsdam_zero_shot/
│   ├── exp_001_default_prompts/
│   │   ├── config.yaml
│   │   ├── logs/
│   │   ├── predictions/
│   │   ├── visualizations/
│   │   ├── metrics/
│   │   └── summary.md
│   ├── exp_002_no_background_prompt/
│   ├── exp_003_prompt_variants/
│   └── exp_004_fusion_ablation/
│
├── potsdam_prompt_ablation/
│   ├── building_prompt_ablation/
│   ├── vegetation_prompt_ablation/
│   └── impervious_prompt_ablation/
│
├── water_reasoning/
│   ├── exp_001_sam3_only/
│   ├── exp_002_sam3_dem_constraint/
│   ├── exp_003_sam3_dem_river_constraint/
│   └── exp_004_full_reasoning/
│
└── paper_tables/
    ├── table_potsdam_zero_shot.csv
    ├── table_prompt_ablation.csv
    ├── table_fusion_ablation.csv
    └── table_water_constraint_ablation.csv
```

每次实验至少保存：

```text
config.yaml
运行日志
预测 mask
彩色预测图
原图 / GT / prediction 对比图
每张图指标 CSV
总体指标 JSON
实验摘要 summary.md
```

这样可以保证论文表格、可视化图和复现实验之间一一对应。

## 5. 实验一：SAM3 + Potsdam 零样本遥感分割基线

### 5.1 实验目的

该实验用于验证 SAM3 的开放词汇分割能力能否迁移到高分辨率遥感图像。

论文中建议表述为：

```text
为了分析 SAM3 在遥感场景中的零样本目标发现能力，本文首先在 ISPRS Potsdam 高分辨率遥感数据集上进行开放词汇分割评估。
```

该实验不是水利实验，而是遥感基础能力验证。

### 5.2 数据输入

输入数据：

```text
Potsdam/2_Ortho_RGB/2_Ortho_RGB/*.tif
```

标签数据：

```text
Potsdam/5_Labels_for_participants_no_Boundary/5_Labels_for_participants_no_Boundary/*.tif
```

推荐快速测试图像：

```text
top_potsdam_2_10
top_potsdam_5_11
top_potsdam_7_9
```

推荐完整验证图像：

```text
全部 24 张带标签 tiles
```

### 5.3 类别与 prompt

| 类别 ID | 类别 | 默认 prompt |
| --- | --- | --- |
| 0 | clutter / background | background clutter |
| 1 | impervious surface | impervious surface |
| 2 | building | building |
| 3 | low vegetation | low vegetation |
| 4 | tree | tree |
| 5 | car | car |

### 5.4 基本流程

```text
读取 RGB 图像
读取并转换标签
将 6000×6000 图像切为 patch
对每个 patch 输入类别 prompt
SAM3 输出 masks、boxes、scores
按类别和置信度融合 masks
合并 patch
计算指标
保存结果
```

### 5.5 需要实现或修复的代码点

#### 5.5.1 标签颜色转类别 ID

Potsdam 标签通常为 RGB 彩色图，必须转换为二维类别图。

建议新增：

```python
COLOR_TO_CLASS = {
    (255, 255, 255): 0,
    (0, 0, 0): 1,
    (0, 0, 255): 2,
    (0, 255, 255): 3,
    (0, 255, 0): 4,
    (255, 255, 0): 5,
}

IGNORE_COLOR = (255, 0, 0)
IGNORE_INDEX = 255
```

标签读取逻辑：

```text
如果标签是 H×W：
    直接使用，但确认取值是否为 0-5

如果标签是 H×W×3：
    根据 RGB 映射到 0-5
    未知颜色或 boundary 设置为 255
```

#### 5.5.2 指标计算排除 ignore

在计算 OA、IoU、F1 前，应先过滤：

```python
valid_mask = ground_truth != IGNORE_INDEX
pred_flat = prediction[valid_mask].flatten()
gt_flat = ground_truth[valid_mask].flatten()
```

否则边界区域会污染指标。

#### 5.5.3 logger 初始化顺序

当前应调整为：

```python
self._setup_logging()

if SAM3_AVAILABLE:
    self._load_sam3_model()
```

#### 5.5.4 SAM3 输出格式兼容

需要统一处理：

```text
torch.Tensor
NumPy array
list
batch 维度
GPU tensor
```

建议封装函数：

```python
def to_numpy(x):
    if isinstance(x, torch.Tensor):
        return x.detach().cpu().numpy()
    return np.asarray(x)
```

#### 5.5.5 patch 合并方式

不能直接对类别 ID 求平均。

推荐优先级：

```text
最佳方案：保存每类 confidence map，重叠区域按 argmax 融合
次优方案：重叠区域 majority vote
临时方案：无重叠 patch，仅用于 smoke test
```

如果使用 confidence map：

```text
每个 patch 输出 class_score_map: C×H×W
整图维护 global_score_map: C×H×W
重叠区域取 max 或平均 score
最终 prediction = argmax(global_score_map)
```

#### 5.5.6 背景类处理

建议实现两种模式：

```text
mode = "background_prompt"
    使用 background clutter prompt 直接预测背景。

mode = "residual_background"
    只预测 1-5 类，未被任何前景覆盖的区域设为 0。
```

论文中可以比较两者差异。

### 5.6 输出指标

基础指标：

```text
Overall Accuracy
Mean IoU
Mean F1
Per-class IoU
Per-class F1
Per-class Precision
Per-class Recall
Confusion Matrix
```

建议额外输出：

```text
每类预测面积
每类 GT 面积
每类面积误差
每张图处理时间
每张图 patch 数量
每个 prompt 的平均 mask 数量
每个 prompt 的平均置信度
```

这些信息有助于分析 SAM3 对不同类别的响应情况。

### 5.7 论文表格

建议生成：

```text
表：SAM3 zero-shot 在 Potsdam quick set 上的结果
表：SAM3 zero-shot 在 Potsdam full set 上的结果
表：各类别 IoU / F1 / Precision / Recall
```

表格字段：

```text
Experiment
Images
OA
mIoU
mF1
IoU-background
IoU-impervious
IoU-building
IoU-low vegetation
IoU-tree
IoU-car
```

## 6. 实验二：Prompt 工程消融实验

### 6.1 实验目的

SAM3 的开放词汇能力高度依赖文本 prompt。该实验用于分析不同 prompt 对遥感类别分割结果的影响。

论文中可用于支撑：

```text
自然语言概念构造会显著影响开放词汇遥感分割结果，因此水利任务中需要任务理解与概念构造模块。
```

### 6.2 实验设计

针对每个类别设置多个 prompt。

#### 不透水面

```text
impervious surface
road and pavement
hard surface
asphalt road
paved area
```

#### 建筑物

```text
building
buildings
rooftop
building roof
house
```

#### 低矮植被

```text
low vegetation
grass
lawn
shrubs
ground vegetation
```

#### 树木

```text
tree
trees
tall tree
tree crown
forest
```

#### 汽车

```text
car
vehicle
cars
small vehicle
parked car
```

### 6.3 代码实现建议

在配置文件中增加：

```yaml
prompt_ablation:
  enabled: true
  target_class: 2
  prompts:
    - "building"
    - "rooftop"
    - "building roof"
```

脚本执行逻辑：

```text
固定其他类别 prompt 不变
每次只替换一个类别 prompt
运行相同测试图像
记录该类别 IoU、F1、Precision、Recall
汇总 prompt 对比表
```

### 6.4 输出结果

保存：

```text
prompt_ablation_results.csv
prompt_ablation_summary.md
每个 prompt 的可视化对比图
```

CSV 字段：

```text
target_class
prompt
OA
mIoU
target_class_iou
target_class_f1
target_class_precision
target_class_recall
avg_num_masks
avg_score
```

### 6.5 论文分析重点

重点分析：

```text
通用概念和遥感专业概念哪个更有效
单数和复数是否影响结果
building 与 rooftop 的差异
tree 与 forest 的差异
car 与 vehicle 的差异
prompt 对小目标和大面积地物的影响
```

## 7. 实验三：背景类处理方式消融

### 7.1 实验目的

开放词汇分割模型通常更适合检测前景概念，而不是直接检测背景。Potsdam 中的 clutter/background 是语义分割标签，不一定是 SAM3 能稳定识别的视觉概念。

该实验用于验证：

```text
背景是否应通过 prompt 直接预测，还是作为前景类别的剩余区域。
```

### 7.2 对比方案

方案 A：

```text
使用 6 个 prompt，包括 background clutter。
```

方案 B：

```text
只使用 1-5 类前景 prompt。
没有被前景 mask 覆盖的区域设为 background。
```

方案 C：

```text
只使用 1-5 类前景 prompt。
结合置信度阈值和最小面积过滤后，剩余区域设为 background。
```

### 7.3 输出指标

重点关注：

```text
background IoU
整体 mIoU
前景类 mIoU
误检面积
未覆盖面积比例
```

建议单独报告：

```text
foreground mIoU = mean(IoU class 1-5)
```

因为背景类可能掩盖前景识别能力。

## 8. 实验四：多类别 mask 融合策略消融

### 8.1 实验目的

SAM3 对每个 prompt 会输出多个实例 mask。不同类别 mask 之间可能重叠，因此需要融合策略。

该实验用于比较不同融合方式对最终语义图的影响。

### 8.2 对比策略

#### 策略一：置信度优先

```text
重叠区域分配给 score 更高的 mask。
```

#### 策略二：类别优先级

```text
按照类别优先级覆盖。
例如：
building > car > tree > low vegetation > impervious > background
```

#### 策略三：置信度 + 类别优先级

```text
先按类别优先级处理小目标和明确目标，再用置信度处理大面积类别。
```

#### 策略四：面积过滤 + 置信度融合

```text
过滤过小或过大的异常 mask，再按置信度融合。
```

#### 策略五：粗分割先验 + SAM3

```text
SegFormer / DeepLabV3+ 提供 coarse semantic map；
SAM3 提供实例边界和开放词汇确认；
融合时同时考虑 coarse logits 和 SAM3 scores。
```

该策略是后续增强版，可作为第二阶段实验。

### 8.3 代码实现建议

新增统一融合接口：

```python
def fuse_masks(predictions, strategy, class_priority=None):
    ...
```

配置：

```yaml
fusion:
  strategy: "confidence"
  priority: [2, 5, 4, 3, 1, 0]
  min_area: 20
  max_area_ratio: 0.8
```

### 8.4 论文分析重点

重点说明：

```text
开放词汇实例分割输出不等同于语义分割输出；
从实例 masks 到语义类别图需要合理融合；
融合策略会显著影响最终 mIoU 和小目标表现。
```

## 9. 实验五：SAM3 + 粗语义分割模型融合

### 9.1 实验目的

SAM3 擅长开放词汇目标发现和 mask 生成，但标准语义分割要求每个像素都有稳定类别。粗语义分割模型可以提供像素级类别先验，SAM3 可以负责边界修正或概念确认。

该实验可以作为论文增强实验，不一定是第一阶段必须完成。

### 9.2 推荐架构

```text
RGB patch
        ↓
SegFormer / DeepLabV3+ 输出 coarse 6 类 logits
        ↓
每类连通域生成 box prompt
        ↓
SAM3 使用 text prompt + box prompt 细化 mask
        ↓
coarse logits + SAM3 scores 融合
        ↓
最终 6 类语义图
```

### 9.3 需要新增代码模块

```text
coarse_segmentation/
├── train_segformer.py
├── infer_segformer.py
├── dataset_potsdam.py
└── checkpoints/

prompt_generation/
├── connected_components.py
├── box_prompt_generator.py
└── mask_prompt_generator.py

fusion/
├── semantic_sam3_fusion.py
└── confidence_fusion.py
```

### 9.4 对比实验

| 方法 | 说明 |
| --- | --- |
| SegFormer only | 粗语义分割基线 |
| SAM3 zero-shot | 仅使用文本 prompt |
| SegFormer + SAM3 boxes | 用 coarse map 生成 box，SAM3 细化 |
| SegFormer + SAM3 + score fusion | 同时融合 coarse logits 和 SAM3 score |

### 9.5 论文价值

该实验可以证明：

```text
SAM3 不是替代所有语义分割模型，而是可以与专用分割模型协同；
粗分割模型负责类别判别，SAM3 负责开放词汇目标确认和边界优化。
```

## 10. 实验六：水利场景推理分割实验

### 10.1 实验目的

该实验是论文最核心的应用支撑，用于证明：

```text
SAM3 + 水利知识约束比单纯 SAM3 zero-shot 更适合水利智能感知任务。
```

如果时间有限，建议至少选择一个水利任务完成。

优先推荐：

```text
水体提取 / 洪水淹没区识别
```

原因：

```text
数据相对容易获取；
与遥感和水利主题高度相关；
可以自然引入 DEM、河网、历史水体等约束；
评价指标较清晰。
```

### 10.2 任务 A：水体提取

#### 输入数据

```text
遥感 RGB / 多光谱影像
可选：NDWI / MNDWI
可选：DEM
可选：河网矢量
可选：历史水体边界
```

#### SAM3 prompts

```text
water
river water
lake water
reservoir water surface
pond
shadow
dark roof
```

#### 基本流程

```text
SAM3 根据 water prompt 生成候选水体 mask
SAM3 根据 shadow / dark roof 生成疑似误检 mask
利用 NDWI 或颜色特征辅助判断
利用河网和历史水体边界筛选
输出最终水体 mask
```

#### 知识约束

```text
与河网或历史水体相交的区域优先保留
高程异常区域剔除
明显建筑物顶部区域剔除
孤立小斑块按面积阈值过滤
```

#### 指标

```text
IoU
F1
Precision
Recall
水体面积相对误差
连通性一致率
```

### 10.3 任务 B：洪水淹没区识别

#### 输入数据

```text
灾后遥感影像
灾前或历史水体边界
DEM
河网矢量
可选：降雨、水位数据
可选：人工淹没范围标签
```

#### SAM3 prompts

```text
flood water
standing water
flooded road
flooded farmland
river water
permanent water body
shadow
dark roof
```

#### 目标定义

```text
新增淹没区 =
当前水体候选区域
- 历史常水体
- 阴影 / 暗色屋顶误检
+ 与河道或低洼区连通的水体
```

#### 推理流程

```text
1. SAM3 提取 current water / flood water 候选 mask
2. SAM3 或规则提取 shadow / dark roof 候选误检区
3. 使用历史水体边界去除永久水体
4. 使用 DEM 筛除高程异常区域
5. 使用河网缓冲区和连通性保留合理洪水区域
6. 输出新增淹没范围和面积统计
7. 生成解释文本
```

#### 对比方法

```text
SAM3 only
SAM3 + prompt engineering
SAM3 + historical water
SAM3 + historical water + DEM
SAM3 + historical water + DEM + river connectivity
```

#### 指标

```text
IoU
F1
Precision
Recall
新增淹没面积误差
关键道路 / 建筑物受灾漏检率
误检面积比例
```

### 10.4 任务 C：河道和岸线识别

#### prompts

```text
main river
tributary
river channel
river bank
shoreline
sandbar
wetland
```

#### 约束

```text
与 GIS 河网方向一致
与历史河道中心线接近
岸线变化不能出现不合理跳变
孤立池塘不应被识别为主河道
```

#### 指标

```text
IoU
岸线平均偏移距离
Hausdorff Distance
水面面积变化误差
主河道连通性一致率
```

### 10.5 任务 D：水工结构缺陷分割

该任务需要近景巡检图像或无人机图像。

#### prompts

```text
concrete crack
dam crack
seepage area
spalling concrete
exposed rebar
erosion scar
slope failure
```

#### 指标

```text
Dice
IoU
缺陷召回率
误报率
边界误差
人工复核评分
```

该任务与遥感水体实验差异较大，如果数据不足，可作为应用展望，不一定作为主实验。

## 11. 水利知识约束模块代码设计

建议将水利知识约束写成独立模块，避免混在 SAM3 推理代码中。

推荐目录：

```text
hydro_constraints/
├── dem_constraint.py
├── river_connectivity.py
├── historical_water.py
├── morphology_filter.py
├── area_statistics.py
└── explanation_generator.py
```

### 11.1 DEM 约束

输入：

```text
候选 mask
DEM / DSM
```

处理：

```text
计算候选区域平均高程、最大高程、局部相对高程
剔除明显高程异常区域
保留低洼区域
```

输出：

```text
dem_valid_mask
dem_constraint_report
```

### 11.2 河网连通性约束

输入：

```text
候选水体 mask
河网矢量或河道 raster
```

处理：

```text
构建河网缓冲区
判断候选区域是否与河网相交
判断候选区域是否通过邻域与河道连通
```

输出：

```text
connectivity_valid_mask
connectivity_score
```

### 11.3 历史水体差分

输入：

```text
当前水体 mask
历史水体 mask
```

处理：

```text
新增水体 = 当前水体 - 历史水体
永久水体 = 当前水体 ∩ 历史水体
```

输出：

```text
new_flood_mask
permanent_water_mask
```

### 11.4 形态学过滤

处理：

```text
连通域分析
面积阈值过滤
孔洞填充
边界平滑
狭长异常区域过滤
```

### 11.5 解释生成

每个候选区域保存约束检查结果：

```text
SAM3 score
是否属于历史水体
是否位于低洼区
是否与河网连通
面积
风险等级
```

再生成解释：

```text
该区域被判定为新增淹没区，因为其被 SAM3 识别为 flood water，
不属于历史常水体，位于 DEM 低洼区，并与主河道连通。
```

## 12. 实验配置文件设计

建议把当前 `config_sam3_evaluation.yaml` 扩展为统一实验配置。

推荐结构：

```yaml
experiment:
  name: "potsdam_zero_shot_default"
  seed: 42
  mode: "quick"

paths:
  base_dir: "Potsdam"
  output_dir: "results/potsdam_zero_shot/exp_001_default_prompts"
  checkpoint_path: "sam3/checkpoints/sam3.1_multiplex.pt"

data:
  dataset: "potsdam"
  image_subdir: "2_Ortho_RGB/2_Ortho_RGB"
  label_subdir: "5_Labels_for_participants_no_Boundary/5_Labels_for_participants_no_Boundary"
  image_pattern: "{image_id}_RGB.tif"
  label_pattern: "{image_id}_label_noBoundary.tif"
  test_images:
    - "top_potsdam_2_10"
    - "top_potsdam_5_11"
    - "top_potsdam_7_9"

model:
  name: "sam3"
  device: "auto"
  use_fp16: true

image_processing:
  patch_size: 1008
  stride: 672

classes:
  ignore_index: 255
  definitions:
    - id: 0
      name: "clutter/background"
      prompt: "background clutter"
      color: [255, 255, 255]
    - id: 1
      name: "impervious surface"
      prompt: "impervious surface"
      color: [0, 0, 0]

inference:
  score_threshold: 0.5
  max_predictions_per_class: 100
  background_mode: "residual_background"

fusion:
  strategy: "confidence"
  min_area: 20
  class_priority: [2, 5, 4, 3, 1, 0]

metrics:
  ignore_index: 255
  compute_confusion_matrix: true

output:
  save_predictions: true
  save_visualizations: true
  save_intermediate: false
```

## 13. 推荐脚本拆分

当前 `sam3_potsdam_evaluation.py` 可以先继续使用，但后续为了论文实验管理，建议逐步拆分。

推荐结构：

```text
scripts/
├── run_potsdam_zero_shot.py
├── run_prompt_ablation.py
├── run_fusion_ablation.py
├── run_water_reasoning.py
├── summarize_results.py
└── make_paper_figures.py

src/
├── datasets/
│   ├── potsdam.py
│   └── water_dataset.py
├── sam3_inference/
│   ├── predictor.py
│   ├── prompt_runner.py
│   └── tensor_utils.py
├── fusion/
│   ├── mask_fusion.py
│   └── patch_merging.py
├── metrics/
│   ├── segmentation_metrics.py
│   └── hydro_metrics.py
├── visualization/
│   ├── color_map.py
│   └── comparison_plot.py
└── hydro_constraints/
    ├── dem_constraint.py
    ├── river_connectivity.py
    └── historical_water.py
```

如果论文时间紧，可以不完全重构，但至少建议新增：

```text
标签转换函数
配置读取函数
结果汇总脚本
prompt 消融脚本
```

## 14. 评价指标设计

### 14.1 通用语义分割指标

用于 Potsdam 和普通分割任务：

```text
Overall Accuracy
Mean IoU
Mean F1
Per-class IoU
Per-class Precision
Per-class Recall
Confusion Matrix
```

### 14.2 边界指标

适合建筑物、岸线、缺陷等边界敏感任务：

```text
Boundary F1
Hausdorff Distance
Average Symmetric Surface Distance
```

如果实现成本高，可先不做。

### 14.3 水利工程指标

用于水体、洪水、河道等任务：

```text
水体面积相对误差
新增淹没面积误差
河网连通性一致率
岸线平均偏移距离
关键风险对象召回率
误检面积比例
```

建议论文至少加入一个工程指标，例如：

```text
Area Error = |Predicted Area - Ground Truth Area| / Ground Truth Area
```

这样可以体现水利工程应用价值，而不是只停留在计算机视觉指标。

## 15. 论文表格规划

### 表 1：Potsdam zero-shot 基线结果

字段：

```text
Method
Prompt setting
Background mode
OA
mIoU
mF1
Foreground mIoU
```

### 表 2：Potsdam 各类别结果

字段：

```text
Class
IoU
F1
Precision
Recall
Predicted Area
GT Area
```

### 表 3：Prompt 消融实验

字段：

```text
Target Class
Prompt
Class IoU
Class F1
Precision
Recall
Avg Score
Avg Num Masks
```

### 表 4：融合策略消融

字段：

```text
Fusion Strategy
OA
mIoU
Foreground mIoU
Small Object IoU
Notes
```

### 表 5：水利知识约束消融

字段：

```text
Method
IoU
F1
Precision
Recall
Area Error
False Positive Area
Connectivity Score
```

## 16. 论文图像规划

### 图 1：Potsdam zero-shot 可视化

每行展示一张图：

```text
RGB image
Ground truth
SAM3 prediction
Overlay
Error map
```

### 图 2：Prompt 对比可视化

例如 building 类：

```text
building prompt
rooftop prompt
building roof prompt
GT building mask
```

### 图 3：背景处理方式对比

```text
background prompt
residual background
GT
error map
```

### 图 4：水利约束校正过程

```text
原始影像
SAM3 water mask
历史水体
DEM 低洼区
河网缓冲区
最终新增淹没区
```

### 图 5：解释输出示例

展示某个水体区域：

```text
mask
box
score
约束检查表
自然语言解释
```

## 17. 实验执行顺序建议

建议按以下顺序推进，避免一开始就做过大的系统。

### 阶段 1：修复 Potsdam 基线

目标：

```text
让 SAM3 + Potsdam zero-shot 结果可信。
```

任务：

```text
修复标签转换
修复 logger
修复 ignore 指标
修复 tensor 输出兼容
修复 patch 融合
支持 YAML 配置
```

验收：

```text
quick 3 张图能稳定跑完
输出 prediction、visualization、metrics
指标计算不报错且标签维度正确
```

### 阶段 2：完成消融实验

目标：

```text
形成论文可用的 Potsdam 表格。
```

任务：

```text
背景 prompt vs residual background
prompt variants
fusion strategies
score threshold sweep
```

验收：

```text
至少生成 3 张论文表格和若干可视化图。
```

### 阶段 3：补充水利场景案例

目标：

```text
让论文从普通遥感分割落到水利应用。
```

任务：

```text
选择一个水利数据或案例
实现 SAM3 water / flood prompt 推理
加入历史水体或 DEM / 河网约束
输出前后对比
```

验收：

```text
有 SAM3 only 与 SAM3 + hydro constraints 的对比结果。
```

### 阶段 4：整理论文结果

目标：

```text
将实验结果整理成论文图表。
```

任务：

```text
汇总 CSV
生成表格
筛选可视化案例
整理失败案例
撰写实验分析
```

## 18. 最小可完成实验组合

如果时间紧，建议最低完成以下实验即可支撑一篇结构完整的论文：

```text
实验 1：SAM3 在 Potsdam quick set 上的 zero-shot baseline
实验 2：background prompt vs residual background
实验 3：prompt 消融，至少覆盖 building、tree、impervious surface
实验 4：水体或洪水案例，比较 SAM3 only 与 SAM3 + 简单水利约束
```

这四个实验可以支撑以下论文结论：

```text
SAM3 具备遥感开放词汇目标发现能力；
prompt 构造会影响分割质量；
开放词汇输出需要合理背景和融合策略；
水利知识约束可以提升结果可靠性和可解释性。
```

## 19. 充分版实验组合

如果时间和算力充足，可以完成：

```text
Potsdam full set zero-shot
prompt variants 全类别消融
score threshold sweep
fusion strategy ablation
SegFormer + SAM3 融合
水体提取实验
洪水淹没区实验
水利约束消融
```

这可以支撑更完整的论文结果章节。

## 20. 当前代码优先修改清单

按照重要性排序：

1. `read_label()` 支持 RGB 标签转类别 ID。
2. `calculate_metrics()` 支持 ignore index。
3. `__init__()` 中先 `_setup_logging()`，再 `_load_sam3_model()`。
4. `predict_patch()` 对 SAM3 输出 tensor 做统一转换。
5. `merge_patches()` 不再对类别 ID 做平均。
6. 增加 `background_mode`，支持 residual background。
7. 增加 YAML 配置读取。
8. 增加实验名和输出目录自动管理。
9. 增加 prompt ablation 批量运行。
10. 增加结果汇总脚本。

## 21. 建议命令接口

后续脚本可以设计成：

```bash
python scripts/run_potsdam_zero_shot.py \
  --config configs/potsdam_zero_shot.yaml
```

```bash
python scripts/run_prompt_ablation.py \
  --config configs/prompt_ablation_building.yaml
```

```bash
python scripts/run_fusion_ablation.py \
  --config configs/fusion_ablation.yaml
```

```bash
python scripts/run_water_reasoning.py \
  --config configs/water_reasoning.yaml
```

```bash
python scripts/summarize_results.py \
  --result-root results \
  --output results/paper_tables
```

## 22. 实验记录规范

每次实验应记录：

```text
实验编号
实验目的
代码版本或 git commit
配置文件
数据集和测试图像
SAM3 权重路径
prompt 设置
patch size / stride
score threshold
fusion strategy
background mode
运行时间
显存占用
主要指标
失败案例
结论摘要
```

推荐每个实验目录保存 `summary.md`：

```markdown
# Experiment Summary

## Purpose

## Config

## Results

## Observations

## Failure Cases

## Conclusion
```

## 23. 失败案例分析

论文中应主动分析失败案例，而不是只展示成功结果。

Potsdam 可能失败点：

```text
tree 与 low vegetation 混淆
impervious surface 与 building roof 混淆
car 小目标漏检
background prompt 产生大面积误检
patch 边缘处预测不连续
阴影区域被误识别为目标
```

水利场景可能失败点：

```text
阴影被识别为水体
暗色屋顶被识别为水体
湿地和浅滩边界模糊
植被覆盖水体漏检
水体与道路积水难以区分
河网或 DEM 数据分辨率不匹配
```

这些失败案例可用于引出：

```text
专业知识约束
多源数据融合
不确定性估计
人工交互修正
```

## 24. 代码实验与论文结论的对应关系

| 论文结论 | 需要的实验支撑 |
| --- | --- |
| SAM3 具备遥感开放词汇目标发现能力 | Potsdam zero-shot |
| Prompt 构造影响分割结果 | Prompt 消融 |
| 背景类不适合直接作为开放词汇目标 | 背景处理消融 |
| 实例 mask 到语义图需要融合策略 | 融合策略消融 |
| SAM3 仍需专业约束才能用于水利任务 | 水利约束实验 |
| 推理分割结果具有可解释性 | 约束检查报告和解释文本 |

## 25. 最终建议

代码实验应围绕一条清晰路线展开：

```text
先把 SAM3 在 Potsdam 上的遥感零样本基线做扎实，
再通过 prompt、背景处理和融合策略说明开放词汇分割的关键影响因素，
最后补充一个水利场景实验，用 DEM、河网或历史水体约束证明本文方法的专业价值。
```

最重要的是，论文实验不要变成简单的“跑模型、报指标”。每个实验都应对应一个明确论点：

```text
zero-shot 实验证明基础能力；
prompt 消融证明概念构造的重要性；
背景和融合实验证明开放词汇输出需要任务化转换；
水利约束实验证明专业知识能提升可靠性和可解释性。
```

这样，代码实验才能真正支撑论文逻辑。
