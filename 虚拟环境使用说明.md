# 虚拟环境使用说明

## 📍 虚拟环境信息

**虚拟环境路径**: `/home/anjou/PythonENV/Test_11/.venv_hf/`

**Python 版本**: 3.12.3

**创建时间**: 2026-05-13

## 🚀 快速启动

### 方法 1: 使用绝对路径（推荐）

```bash
# 直接使用虚拟环境中的 Python
cd /home/anjou/PythonENV/Test_11
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 sam3_potsdam_evaluation.py

# 或者指定配置文件
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 sam3_potsdam_evaluation.py --config config_sam3_evaluation.yaml
```

### 方法 2: 激活虚拟环境

```bash
# 激活虚拟环境
source /home/anjou/PythonENV/Test_11/.venv_hf/bin/activate

# 然后可以直接使用 python
python sam3_potsdam_evaluation.py

# 完成后退出虚拟环境
deactivate
```

### 方法 3: 运行测试脚本

```bash
cd /home/anjou/PythonENV/Test_11
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 test_fixes.py
```

## 📦 已安装的主要包

| 包名 | 版本 | 用途 |
|------|------|------|
| numpy | 2.4.4 | 数值计算 |
| torch | - | 深度学习框架 |
| matplotlib | 3.10.9 | 数据可视化 |
| pandas | 3.0.3 | 数据处理 |
| scikit-learn | 1.8.0 | 机器学习指标 |
| opencv-python | 4.13.0.92 | 图像处理 |
| PIL | 12.2.0 | 图像读取 |
| scipy | 1.17.1 | 科学计算 |

## 🔧 常用命令

### 检查环境状态
```bash
# 检查虚拟环境中的包
/home/anjou/PythonENV/Test_11/.venv_hf/bin/pip list

# 检查Python版本
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 --version
```

### 安装新包
```bash
# 方法1: 激活后安装
source /home/anjou/PythonENV/Test_11/.venv_hf/bin/activate
pip install package_name

# 方法2: 直接使用虚拟环境的pip
/home/anjou/PythonENV/Test_11/.venv_hf/bin/pip install package_name
```

### 运行评估
```bash
# 使用默认配置
cd /home/anjou/PythonENV/Test_11
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 sam3_potsdam_evaluation.py

# 使用自定义配置
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 sam3_potsdam_evaluation.py --config your_config.yaml
```

## 📂 项目结构

```
/home/anjou/PythonENV/Test_11/
├── .venv_hf/                      # 虚拟环境目录
│   ├── bin/                       # 可执行文件
│   │   ├── python3                # Python 解释器
│   │   ├── pip                    # 包管理器
│   │   └── activate               # 激活脚本
│   └── lib/                       # 已安装的包
├── sam3_potsdam_evaluation.py     # 主评估脚本
├── test_fixes.py                  # 测试脚本
├── config_sam3_evaluation.yaml    # 配置文件
├── Potsdam/                       # 数据集目录
├── sam3/                          # SAM3 模型目录
└── results/                       # 输出结果目录
```

## ⚡ 快速测试

验证虚拟环境是否正常工作：

```bash
# 运行测试脚本
cd /home/anjou/PythonENV/Test_11
/home/anjou/PythonENV/Test_11/.venv_hf/bin/python3 test_fixes.py

# 预期输出: 所有测试通过 (4/4)
```

## 🐛 常见问题

### Q: 为什么不直接使用系统 Python？
A: 项目依赖特定的包版本，虚拟环境可以隔离依赖，避免冲突。

### Q: 如何知道当前是否在虚拟环境中？
A: 命令行提示符前会显示 `(.venv_hf)`，或者使用 `which python` 检查路径。

### Q: 忘记激活虚拟环境会怎样？
A: 可能会找不到依赖包或使用错误的 Python 版本，导致程序运行失败。

### Q: 可以在不同目录中使用虚拟环境吗？
A: 可以，但需要使用绝对路径或先切换到项目目录。

## 📝 最佳实践

1. **使用绝对路径**: 在脚本和自动化任务中使用绝对路径更可靠
2. **激活后使用**: 交互式开发时建议激活虚拟环境
3. **定期更新**: 定期更新虚拟环境中的包以获得最新功能
4. **文档记录**: 在项目文档中记录虚拟环境的具体路径和版本信息

## 🎯 相关文件

- **主脚本**: `sam3_potsdam_evaluation.py`
- **配置文件**: `config_sam3_evaluation.yaml`
- **测试脚本**: `test_fixes.py`
- **处理报告**: `服务器侧处理完成报告.md`

---

**最后更新**: 2026-05-15
**维护者**: Claude Code Assistant